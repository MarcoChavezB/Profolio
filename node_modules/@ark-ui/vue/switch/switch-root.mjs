import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { SwitchProvider } from './switch-context.mjs';
import { props, emits } from './switch.props.mjs';
import { useSwitch } from './use-switch.mjs';

const SwitchRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useSwitch(props, emit);
  SwitchProvider(api);
  return () => createVNode(ark.label, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'SwitchRoot',
  props,
  emits
});

export { SwitchRoot };
