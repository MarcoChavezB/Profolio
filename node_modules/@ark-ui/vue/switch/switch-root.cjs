'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const switchContext = require('./switch-context.cjs');
const switch_props = require('./switch.props.cjs');
const useSwitch = require('./use-switch.cjs');

const SwitchRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useSwitch.useSwitch(props, emit);
  switchContext.SwitchProvider(api);
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'SwitchRoot',
  props: switch_props.props,
  emits: switch_props.emits
});

exports.SwitchRoot = SwitchRoot;
