import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSwitchContext } from './switch-context.mjs';

const SwitchControl = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useSwitchContext();
  return () => createVNode(Fragment, null, [createVNode(ark.span, mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  }), createVNode("input", api.value.hiddenInputProps, null)]);
}, {
  name: 'SwitchControl'
});

export { SwitchControl };
