'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const switchContext = require('./switch-context.cjs');

const SwitchLabel = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = switchContext.useSwitchContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SwitchLabel'
});

exports.SwitchLabel = SwitchLabel;
