'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');

const SelectLabel = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectLabel'
});

exports.SelectLabel = SelectLabel;
