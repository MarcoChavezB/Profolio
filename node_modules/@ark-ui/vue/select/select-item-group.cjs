'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');

const SelectItemGroup = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemGroupProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectItemGroup',
  props: {
    id: {
      type: String,
      required: true
    }
  }
});

exports.SelectItemGroup = SelectItemGroup;
