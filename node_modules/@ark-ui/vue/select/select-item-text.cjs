'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');
const selectItemContext = require('./select-item-context.cjs');

const SelectItemText = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  const itemProps = selectItemContext.useSelectItemContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.getItemTextProps(itemProps), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectItemText'
});

exports.SelectItemText = SelectItemText;
