'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');
const selectItemContext = require('./select-item-context.cjs');

const SelectItemIndicator = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  const itemProps = selectItemContext.useSelectItemContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemIndicatorProps(itemProps), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectItemIndicator'
});

exports.SelectItemIndicator = SelectItemIndicator;
