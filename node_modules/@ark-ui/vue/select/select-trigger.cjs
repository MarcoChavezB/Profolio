'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');

const SelectTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.triggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectTrigger'
});

exports.SelectTrigger = SelectTrigger;
