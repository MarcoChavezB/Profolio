'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');

const SelectControl = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  const isValueEmpty = vue.computed(() => api.value.value.length === 0);
  return () => vue.createVNode(vue.Fragment, null, [vue.createVNode(factory.ark.div, vue.mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  }), vue.createVNode("select", api.value.hiddenSelectProps, [isValueEmpty.value && vue.createVNode("option", {
    "value": ""
  }, null), api.value.collection.toArray().map(option => vue.createVNode("option", {
    "key": option.value,
    "value": option.value
  }, [option.label]))])]);
}, {
  name: 'SelectControl'
});

exports.SelectControl = SelectControl;
