'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');

const SelectItemGroupLabel = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemGroupLabelProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectItemGroupLabel',
  props: {
    htmlFor: {
      type: String,
      required: true
    }
  }
});

exports.SelectItemGroupLabel = SelectItemGroupLabel;
