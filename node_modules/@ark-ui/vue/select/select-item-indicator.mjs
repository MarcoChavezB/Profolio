import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSelectContext } from './select-context.mjs';
import { useSelectItemContext } from './select-item-context.mjs';

const SelectItemIndicator = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useSelectContext();
  const itemProps = useSelectItemContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemIndicatorProps(itemProps), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectItemIndicator'
});

export { SelectItemIndicator };
