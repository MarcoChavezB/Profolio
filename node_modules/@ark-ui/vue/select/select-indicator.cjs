'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');

const SelectIndicator = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.indicatorProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectIndicator'
});

exports.SelectIndicator = SelectIndicator;
