import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSelectContext } from './select-context.mjs';

const SelectItemGroupLabel = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useSelectContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemGroupLabelProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectItemGroupLabel',
  props: {
    htmlFor: {
      type: String,
      required: true
    }
  }
});

export { SelectItemGroupLabel };
