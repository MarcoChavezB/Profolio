import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSelectContext } from './select-context.mjs';
import { useSelectItemContext } from './select-item-context.mjs';

const SelectItemText = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useSelectContext();
  const itemProps = useSelectItemContext();
  return () => createVNode(ark.span, mergeProps(api.value.getItemTextProps(itemProps), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectItemText'
});

export { SelectItemText };
