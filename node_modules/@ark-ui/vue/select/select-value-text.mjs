import { defineComponent, createVNode, mergeProps } from 'vue';
import { selectAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { useSelectContext } from './select-context.mjs';

const SelectValueText = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useSelectContext();
  return () => createVNode(ark.span, mergeProps(selectAnatomy.build().valueText.attrs, attrs), {
    default: () => [slots.default?.() || api.value.valueAsString || props.placeholder]
  });
}, {
  name: 'SelectValueText',
  props: {
    placeholder: {
      type: String
    }
  }
});

export { SelectValueText };
