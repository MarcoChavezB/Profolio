'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');
const selectItemContext = require('./select-item-context.cjs');

const SelectItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  selectItemContext.SelectItemProvider(props);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemState(props))]
  });
}, {
  name: 'SelectItem',
  props: {
    item: {
      type: Object,
      required: true
    }
  }
});

exports.SelectItem = SelectItem;
