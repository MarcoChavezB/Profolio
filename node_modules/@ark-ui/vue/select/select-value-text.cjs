'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const anatomy = require('@ark-ui/anatomy');
const factory = require('../factory.cjs');
const selectContext = require('./select-context.cjs');

const SelectValueText = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = selectContext.useSelectContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(anatomy.selectAnatomy.build().valueText.attrs, attrs), {
    default: () => [slots.default?.() || api.value.valueAsString || props.placeholder]
  });
}, {
  name: 'SelectValueText',
  props: {
    placeholder: {
      type: String
    }
  }
});

exports.SelectValueText = SelectValueText;
