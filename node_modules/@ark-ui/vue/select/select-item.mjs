import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSelectContext } from './select-context.mjs';
import { SelectItemProvider } from './select-item-context.mjs';

const SelectItem = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useSelectContext();
  SelectItemProvider(props);
  return () => createVNode(ark.div, mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemState(props))]
  });
}, {
  name: 'SelectItem',
  props: {
    item: {
      type: Object,
      required: true
    }
  }
});

export { SelectItem };
