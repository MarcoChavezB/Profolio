import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSelectContext } from './select-context.mjs';

const SelectClearTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useSelectContext();
  return () => createVNode(ark.button, mergeProps(api.value.clearTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectClearTrigger'
});

export { SelectClearTrigger };
