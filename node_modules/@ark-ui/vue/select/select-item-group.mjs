import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSelectContext } from './select-context.mjs';

const SelectItemGroup = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useSelectContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemGroupProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SelectItemGroup',
  props: {
    id: {
      type: String,
      required: true
    }
  }
});

export { SelectItemGroup };
