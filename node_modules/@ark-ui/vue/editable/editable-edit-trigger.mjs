import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useEditableContext } from './editable-context.mjs';

const EditableEditTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useEditableContext();
  return () => createVNode(ark.button, mergeProps(api.value.editTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'EditableEditTrigger'
});

export { EditableEditTrigger };
