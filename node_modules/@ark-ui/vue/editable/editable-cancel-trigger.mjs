import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useEditableContext } from './editable-context.mjs';

const EditableCancelTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useEditableContext();
  return () => createVNode(ark.button, mergeProps(api.value.cancelTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'EditableCancelTrigger'
});

export { EditableCancelTrigger };
