'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const editableContext = require('./editable-context.cjs');

const EditableInput = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = editableContext.useEditableContext();
  return () => vue.createVNode(factory.ark.input, vue.mergeProps(api.value.inputProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'EditableInput'
});

exports.EditableInput = EditableInput;
