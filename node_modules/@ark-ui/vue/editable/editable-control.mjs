import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useEditableContext } from './editable-context.mjs';

const EditableControl = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useEditableContext();
  return () => createVNode(ark.div, mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'EditableControl'
});

export { EditableControl };
