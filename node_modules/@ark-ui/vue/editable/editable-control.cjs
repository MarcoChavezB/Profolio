'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const editableContext = require('./editable-context.cjs');

const EditableControl = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = editableContext.useEditableContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'EditableControl'
});

exports.EditableControl = EditableControl;
