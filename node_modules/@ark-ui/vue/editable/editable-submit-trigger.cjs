'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const editableContext = require('./editable-context.cjs');

const EditableSubmitTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = editableContext.useEditableContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.submitTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'EditableSubmitTrigger'
});

exports.EditableSubmitTrigger = EditableSubmitTrigger;
