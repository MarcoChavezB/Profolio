import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useEditableContext } from './editable-context.mjs';

const EditableSubmitTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useEditableContext();
  return () => createVNode(ark.button, mergeProps(api.value.submitTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'EditableSubmitTrigger'
});

export { EditableSubmitTrigger };
