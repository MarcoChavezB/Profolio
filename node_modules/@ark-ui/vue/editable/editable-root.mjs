import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { EditableProvider } from './editable-context.mjs';
import { props, emits } from './editable.props.mjs';
import { useEditable } from './use-editable.mjs';

const EditableRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useEditable(props, emit);
  EditableProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'EditableRoot',
  props,
  emits
});

export { EditableRoot };
