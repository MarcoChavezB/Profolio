'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const editableContext = require('./editable-context.cjs');

const EditablePreview = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = editableContext.useEditableContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.previewProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'EditablePreview'
});

exports.EditablePreview = EditablePreview;
