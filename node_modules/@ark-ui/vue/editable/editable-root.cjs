'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const editableContext = require('./editable-context.cjs');
const editable_props = require('./editable.props.cjs');
const useEditable = require('./use-editable.cjs');

const EditableRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useEditable.useEditable(props, emit);
  editableContext.EditableProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'EditableRoot',
  props: editable_props.props,
  emits: editable_props.emits
});

exports.EditableRoot = EditableRoot;
