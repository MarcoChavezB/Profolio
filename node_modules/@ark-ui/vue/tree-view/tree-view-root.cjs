'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const treeViewContext = require('./tree-view-context.cjs');
const treeView_props = require('./tree-view.props.cjs');
const useTreeView = require('./use-tree-view.cjs');

const TreeViewRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useTreeView.useTreeView(props, emit);
  treeViewContext.TreeViewProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'TreeViewRoot',
  props: treeView_props.props,
  emits: treeView_props.emits
});

exports.TreeViewRoot = TreeViewRoot;
