import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTreeViewContext } from './tree-view-context.mjs';

const TreeViewBranch = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useTreeViewContext();
  return () => createVNode(ark.li, mergeProps(api.value.getBranchProps(props), attrs), {
    default: () => [slots.default?.(api.value.getBranchState(props))]
  });
}, {
  name: 'TreeViewBranch',
  props: {
    depth: {
      type: Number,
      required: true
    },
    id: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

export { TreeViewBranch };
