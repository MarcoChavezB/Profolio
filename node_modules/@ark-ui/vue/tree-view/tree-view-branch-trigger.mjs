import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTreeViewContext } from './tree-view-context.mjs';

const TreeViewBranchTrigger = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useTreeViewContext();
  return () => createVNode(ark.button, mergeProps(api.value.getBranchTriggerProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TreeViewBranchTrigger',
  props: {
    depth: {
      type: Number,
      required: true
    },
    id: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

export { TreeViewBranchTrigger };
