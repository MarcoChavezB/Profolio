import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { TreeViewProvider } from './tree-view-context.mjs';
import { props, emits } from './tree-view.props.mjs';
import { useTreeView } from './use-tree-view.mjs';

const TreeViewRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useTreeView(props, emit);
  TreeViewProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'TreeViewRoot',
  props,
  emits
});

export { TreeViewRoot };
