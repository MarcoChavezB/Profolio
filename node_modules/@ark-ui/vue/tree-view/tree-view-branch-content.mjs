import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTreeViewContext } from './tree-view-context.mjs';

const TreeViewBranchContent = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useTreeViewContext();
  return () => createVNode(ark.ul, mergeProps(api.value.getBranchContentProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TreeViewBranchContent',
  props: {
    depth: {
      type: Number,
      required: true
    },
    id: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

export { TreeViewBranchContent };
