import { defineComponent, createVNode, mergeProps } from 'vue';
import { treeViewAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { useTreeViewContext } from './tree-view-context.mjs';

const TreeViewBranchIndicator = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useTreeViewContext();
  return () => createVNode(ark.div, mergeProps(api.value.getBranchProps(props), treeViewAnatomy.build().branchIndicator.attrs, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TreeViewBranchIndicator',
  props: {
    depth: {
      type: Number,
      required: true
    },
    id: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

export { TreeViewBranchIndicator };
