import { defineComponent, createVNode, mergeProps } from 'vue';
import { treeViewAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { useTreeViewContext } from './tree-view-context.mjs';

const TreeViewItemText = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useTreeViewContext();
  return () => createVNode(ark.span, mergeProps(api.value.getItemProps(props), treeViewAnatomy.build().itemText.attrs, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TreeViewItemText',
  props: {
    depth: {
      type: Number,
      required: true
    },
    id: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

export { TreeViewItemText };
