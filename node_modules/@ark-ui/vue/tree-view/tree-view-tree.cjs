'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const treeViewContext = require('./tree-view-context.cjs');

const TreeViewTree = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = treeViewContext.useTreeViewContext();
  return () => vue.createVNode(factory.ark.ul, vue.mergeProps(api.value.treeProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TreeViewTree'
});

exports.TreeViewTree = TreeViewTree;
