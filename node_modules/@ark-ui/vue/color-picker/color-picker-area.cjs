'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const colorPickerAreaContext = require('./color-picker-area-context.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

const ColorPickerArea = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  const areaProps = vue.computed(() => ({
    xChannel: props.xChannel,
    yChannel: props.yChannel
  }));
  colorPickerAreaContext.ColorPickerAreaProvider(areaProps);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getAreaProps(areaProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerArea',
  props: {
    xChannel: {
      type: String
    },
    yChannel: {
      type: String
    }
  }
});

exports.ColorPickerArea = ColorPickerArea;
