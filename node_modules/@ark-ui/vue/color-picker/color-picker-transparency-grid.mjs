import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerTransparencyGrid = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  return () => createVNode(ark.div, mergeProps(api.value.getTransparencyGridProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerTransparencyGrid',
  props: {
    size: {
      type: String
    }
  }
});

export { ColorPickerTransparencyGrid };
