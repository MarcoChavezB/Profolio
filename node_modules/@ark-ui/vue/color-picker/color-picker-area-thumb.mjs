import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useColorPickerAreaContext } from './color-picker-area-context.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerAreaThumb = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  const areaProps = useColorPickerAreaContext();
  return () => createVNode(ark.div, mergeProps(api.value.getAreaThumbProps(areaProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerAreaThumb'
});

export { ColorPickerAreaThumb };
