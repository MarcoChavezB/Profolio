import { defineComponent, createVNode, mergeProps } from 'vue';
import { colorPickerAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';

const ColorPickerSwatchGroup = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  return () => createVNode(ark.div, mergeProps(colorPickerAnatomy.build().swatchGroup.attrs, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerSwatchGroup'
});

export { ColorPickerSwatchGroup };
