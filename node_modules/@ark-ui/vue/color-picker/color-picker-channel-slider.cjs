'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const colorPickerChannelSliderContext = require('./color-picker-channel-slider-context.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

const ColorPickerChannelSlider = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  colorPickerChannelSliderContext.ColorPickerChannelSliderProvider(vue.reactive(props));
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getChannelSliderProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerChannelSlider',
  props: {
    channel: {
      type: String,
      required: true
    },
    orientation: {
      type: String
    }
  }
});

exports.ColorPickerChannelSlider = ColorPickerChannelSlider;
