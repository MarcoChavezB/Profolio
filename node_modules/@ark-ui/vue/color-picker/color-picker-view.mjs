import { defineComponent, createVNode, mergeProps } from 'vue';
import { colorPickerAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerView = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  return () => api.value.format !== props.format ? null : createVNode(ark.div, mergeProps({
    "data-format": props.format
  }, colorPickerAnatomy.build().view.attrs, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerView',
  props: {
    format: {
      type: String,
      required: true
    }
  }
});

export { ColorPickerView };
