'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

const ColorPickerTransparencyGrid = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getTransparencyGridProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerTransparencyGrid',
  props: {
    size: {
      type: String
    }
  }
});

exports.ColorPickerTransparencyGrid = ColorPickerTransparencyGrid;
