import { defineComponent, reactive, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';
import { ColorPickerSwatchProvider } from './color-picker-swatch-context.mjs';

const ColorPickerSwatch = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  ColorPickerSwatchProvider(reactive(props));
  return () => createVNode(ark.div, mergeProps(api.value.getSwatchProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerSwatch',
  props: {
    readOnly: {
      type: Boolean,
      default: undefined
    },
    value: {
      type: [String, Object],
      required: true
    }
  }
});

export { ColorPickerSwatch };
