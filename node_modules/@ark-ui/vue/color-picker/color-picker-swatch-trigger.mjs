import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerSwatchTrigger = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  return () => createVNode(ark.button, mergeProps(api.value.getSwatchTriggerProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerSwatchTrigger',
  props: {
    value: {
      type: [String, Object],
      required: true
    },
    disabled: {
      type: [String, Object]
    }
  }
});

export { ColorPickerSwatchTrigger };
