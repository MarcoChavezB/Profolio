import { defineComponent, computed, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { ColorPickerAreaProvider } from './color-picker-area-context.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerArea = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  const areaProps = computed(() => ({
    xChannel: props.xChannel,
    yChannel: props.yChannel
  }));
  ColorPickerAreaProvider(areaProps);
  return () => createVNode(ark.div, mergeProps(api.value.getAreaProps(areaProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerArea',
  props: {
    xChannel: {
      type: String
    },
    yChannel: {
      type: String
    }
  }
});

export { ColorPickerArea };
