'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

const ColorPickerControl = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerControl'
});

exports.ColorPickerControl = ColorPickerControl;
