import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerEyeDropperTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  return () => createVNode(ark.button, mergeProps(api.value.eyeDropperTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerEyeDropperTrigger'
});

export { ColorPickerEyeDropperTrigger };
