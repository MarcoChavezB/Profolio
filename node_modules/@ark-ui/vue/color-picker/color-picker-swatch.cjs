'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const colorPickerContext = require('./color-picker-context.cjs');
const colorPickerSwatchContext = require('./color-picker-swatch-context.cjs');

const ColorPickerSwatch = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  colorPickerSwatchContext.ColorPickerSwatchProvider(vue.reactive(props));
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getSwatchProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerSwatch',
  props: {
    readOnly: {
      type: Boolean,
      default: undefined
    },
    value: {
      type: [String, Object],
      required: true
    }
  }
});

exports.ColorPickerSwatch = ColorPickerSwatch;
