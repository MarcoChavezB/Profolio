'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const anatomy = require('@ark-ui/anatomy');
const factory = require('../factory.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

const ColorPickerView = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  return () => api.value.format !== props.format ? null : vue.createVNode(factory.ark.div, vue.mergeProps({
    "data-format": props.format
  }, anatomy.colorPickerAnatomy.build().view.attrs, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerView',
  props: {
    format: {
      type: String,
      required: true
    }
  }
});

exports.ColorPickerView = ColorPickerView;
