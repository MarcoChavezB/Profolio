'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

function _isSlot(s) {
  return typeof s === 'function' || Object.prototype.toString.call(s) === '[object Object]' && !vue.isVNode(s);
}
const ColorPickerFormatSelect = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  return () => {
    let _slot;
    return vue.createVNode(factory.ark.select, vue.mergeProps(api.value.formatSelectProps, attrs), _isSlot(_slot = ['rgba', 'hsla', 'hsba'].map(format => vue.createVNode(factory.ark.option, {
      "key": format,
      "value": format
    }, _isSlot(format) ? format : {
      default: () => [format]
    }))) ? _slot : {
      default: () => [_slot]
    });
  };
}, {
  name: 'ColorPickerFormatSelect'
});

exports.ColorPickerFormatSelect = ColorPickerFormatSelect;
