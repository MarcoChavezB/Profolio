'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

const ColorPickerLabel = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerLabel'
});

exports.ColorPickerLabel = ColorPickerLabel;
