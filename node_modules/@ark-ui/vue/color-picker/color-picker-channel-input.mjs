import { defineComponent, computed, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerChannelInput = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  const channelProps = computed(() => ({
    channel: props.channel,
    orientation: props.orientation
  }));
  return () => createVNode(ark.input, mergeProps(api.value.getChannelInputProps(channelProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerChannelInput',
  props: {
    channel: {
      type: String,
      required: true
    },
    orientation: {
      type: String
    }
  }
});

export { ColorPickerChannelInput };
