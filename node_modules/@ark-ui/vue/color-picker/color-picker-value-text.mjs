import { defineComponent, createVNode, mergeProps } from 'vue';
import { colorPickerAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerValueText = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  return () => createVNode(ark.span, mergeProps(colorPickerAnatomy.build().valueText.attrs, attrs), {
    default: () => [slots.default?.() || api.value.valueAsString]
  });
}, {
  name: 'ColorPickerValueText',
  props: {
    placeholder: {
      type: String
    }
  }
});

export { ColorPickerValueText };
