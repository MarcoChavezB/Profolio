'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

const ColorPickerChannelInput = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  const channelProps = vue.computed(() => ({
    channel: props.channel,
    orientation: props.orientation
  }));
  return () => vue.createVNode(factory.ark.input, vue.mergeProps(api.value.getChannelInputProps(channelProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerChannelInput',
  props: {
    channel: {
      type: String,
      required: true
    },
    orientation: {
      type: String
    }
  }
});

exports.ColorPickerChannelInput = ColorPickerChannelInput;
