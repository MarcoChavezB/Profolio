import { defineComponent, reactive, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { ColorPickerChannelSliderProvider } from './color-picker-channel-slider-context.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerChannelSlider = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useColorPickerContext();
  ColorPickerChannelSliderProvider(reactive(props));
  return () => createVNode(ark.div, mergeProps(api.value.getChannelSliderProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ColorPickerChannelSlider',
  props: {
    channel: {
      type: String,
      required: true
    },
    orientation: {
      type: String
    }
  }
});

export { ColorPickerChannelSlider };
