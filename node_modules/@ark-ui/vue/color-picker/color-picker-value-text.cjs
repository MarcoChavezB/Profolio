'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const anatomy = require('@ark-ui/anatomy');
const factory = require('../factory.cjs');
const colorPickerContext = require('./color-picker-context.cjs');

const ColorPickerValueText = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = colorPickerContext.useColorPickerContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(anatomy.colorPickerAnatomy.build().valueText.attrs, attrs), {
    default: () => [slots.default?.() || api.value.valueAsString]
  });
}, {
  name: 'ColorPickerValueText',
  props: {
    placeholder: {
      type: String
    }
  }
});

exports.ColorPickerValueText = ColorPickerValueText;
