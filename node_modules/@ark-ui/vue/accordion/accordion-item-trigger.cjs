'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const presenceContext = require('../presence/presence-context.cjs');
const accordionContext = require('./accordion-context.cjs');
const accordionItemContext = require('./accordion-item-context.cjs');

const AccordionItemTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = accordionContext.useAccordionContext();
  const item = accordionItemContext.useAccordionItemContext();
  const presenceApi = presenceContext.usePresenceContext();
  const triggerProps = vue.computed(() => api.value.getItemTriggerProps(item.value));
  const ariaProps = vue.computed(() => ({
    'aria-controls': presenceApi.value.isUnmounted ? undefined : triggerProps.value['aria-controls']
  }));
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(triggerProps.value, ariaProps.value, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'AccordionItemTrigger'
});

exports.AccordionItemTrigger = AccordionItemTrigger;
