import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useAccordionContext } from './accordion-context.mjs';
import { useAccordionItemContext } from './accordion-item-context.mjs';

const AccordionItemIndicator = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  const api = useAccordionContext();
  const item = useAccordionItemContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemIndicatorProps(item.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'AccordionItemIndicator'
});

export { AccordionItemIndicator };
