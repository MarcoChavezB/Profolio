import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useNumberInputContext } from './number-input-context.mjs';

const NumberInputLabel = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useNumberInputContext();
  return () => createVNode(ark.label, mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'NumberInputLabel'
});

export { NumberInputLabel };
