'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const numberInputContext = require('./number-input-context.cjs');
const numberInput_props = require('./number-input.props.cjs');
const useNumberInput = require('./use-number-input.cjs');

const NumberInputRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useNumberInput.useNumberInput(props, emit);
  numberInputContext.NumberInputProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'NumberInputRoot',
  props: numberInput_props.props,
  emits: numberInput_props.emits
});

exports.NumberInputRoot = NumberInputRoot;
