import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useNumberInputContext } from './number-input-context.mjs';

const NumberInputIncrementTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useNumberInputContext();
  return () => createVNode(ark.button, mergeProps(api.value.incrementTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'NumberInputIncrementTrigger'
});

export { NumberInputIncrementTrigger };
