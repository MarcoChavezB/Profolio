import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useNumberInputContext } from './number-input-context.mjs';

const NumberInputDecrementTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useNumberInputContext();
  return () => createVNode(ark.button, mergeProps(api.value.decrementTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'NumberInputDecrementTrigger'
});

export { NumberInputDecrementTrigger };
