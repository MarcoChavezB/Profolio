import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { NumberInputProvider } from './number-input-context.mjs';
import { props, emits } from './number-input.props.mjs';
import { useNumberInput } from './use-number-input.mjs';

const NumberInputRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useNumberInput(props, emit);
  NumberInputProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'NumberInputRoot',
  props,
  emits
});

export { NumberInputRoot };
