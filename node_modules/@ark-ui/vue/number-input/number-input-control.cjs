'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const numberInputContext = require('./number-input-context.cjs');

const NumberInputControl = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = numberInputContext.useNumberInputContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'NumberInputControl'
});

exports.NumberInputControl = NumberInputControl;
