'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const numberInputContext = require('./number-input-context.cjs');

const NumberInputLabel = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = numberInputContext.useNumberInputContext();
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'NumberInputLabel'
});

exports.NumberInputLabel = NumberInputLabel;
