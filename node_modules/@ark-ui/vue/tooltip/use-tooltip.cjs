'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const tooltip = require('@zag-js/tooltip');
const vue$1 = require('@zag-js/vue');
const vue = require('vue');
const environmentContext = require('../environment/environment-context.cjs');
const utils = require('../utils.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const tooltip__namespace = /*#__PURE__*/_interopNamespaceDefault(tooltip);

const useTooltip = (props, emit) => {
  const reactiveContext = vue.reactive(props);
  const getRootNode = environmentContext.useEnvironmentContext();
  const [state, send] = vue$1.useMachine(
    tooltip__namespace.machine({
      ...reactiveContext,
      id: reactiveContext.id ?? utils.useId().value,
      getRootNode,
      onOpenChange: (details) => {
        emit("open-change", details);
      }
    })
  );
  const api = vue.computed(() => tooltip__namespace.connect(state.value, send, vue$1.normalizeProps));
  vue.watch(
    () => reactiveContext.open,
    (value) => {
      if (value === void 0)
        return;
      if (value) {
        api.value.open();
      } else {
        api.value.close();
      }
    }
  );
  return api;
};

exports.useTooltip = useTooltip;
