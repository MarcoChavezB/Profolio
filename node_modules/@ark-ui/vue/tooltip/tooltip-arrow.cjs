'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const tooltipContext = require('./tooltip-context.cjs');

const TooltipArrow = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = tooltipContext.useTooltipContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.arrowProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TooltipArrow'
});

exports.TooltipArrow = TooltipArrow;
