import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTooltipContext } from './tooltip-context.mjs';

const TooltipArrowTip = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useTooltipContext();
  return () => createVNode(ark.div, mergeProps(api.value.arrowTipProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TooltipArrowTip'
});

export { TooltipArrowTip };
