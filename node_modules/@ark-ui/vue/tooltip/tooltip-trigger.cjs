'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const tooltipContext = require('./tooltip-context.cjs');

const TooltipTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = tooltipContext.useTooltipContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.triggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TooltipTrigger'
});

exports.TooltipTrigger = TooltipTrigger;
