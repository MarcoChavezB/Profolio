'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const presence_props = require('./presence.props.cjs');
const usePresence = require('./use-presence.cjs');

const Presence = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = usePresence.usePresence(props, emit);
  return () => api.value.isUnmounted ? null : vue.createVNode(factory.ark.div, vue.mergeProps(attrs, api.value.presenceProps, {
    "data-scope": "presence",
    "data-part": "root"
  }), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'Presence',
  props: presence_props.props
});

exports.Presence = Presence;
