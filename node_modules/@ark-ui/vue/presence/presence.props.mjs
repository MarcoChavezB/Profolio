import { declareEmits } from '../utils.mjs';

const props = {
  present: {
    type: Boolean,
    default: void 0
  },
  lazyMount: {
    type: Boolean,
    default: false
  },
  unmountOnExit: {
    type: Boolean,
    default: false
  }
};
const emits = declareEmits(["exit-complete"]);

export { emits, props };
