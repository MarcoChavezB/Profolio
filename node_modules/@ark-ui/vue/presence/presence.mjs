import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { props } from './presence.props.mjs';
import { usePresence } from './use-presence.mjs';

const Presence = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = usePresence(props, emit);
  return () => api.value.isUnmounted ? null : createVNode(ark.div, mergeProps(attrs, api.value.presenceProps, {
    "data-scope": "presence",
    "data-part": "root"
  }), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'Presence',
  props
});

export { Presence };
