'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');
const progress_props = require('./progress.props.cjs');
const useProgress = require('./use-progress.cjs');

const ProgressRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useProgress.useProgress(props);
  progressContext.ProgressProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'ProgressRoot',
  props: progress_props.props,
  emits: progress_props.emits
});

exports.ProgressRoot = ProgressRoot;
