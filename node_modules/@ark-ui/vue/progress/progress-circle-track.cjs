'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');

const ProgressCircleTrack = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = progressContext.useProgressContext();
  return () => vue.createVNode(factory.ark.circle, vue.mergeProps(api.value.circleTrackProps, attrs), null);
}, {
  name: 'ProgressCircleTrack'
});

exports.ProgressCircleTrack = ProgressCircleTrack;
