'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');

const ProgressCircle = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = progressContext.useProgressContext();
  return () => vue.createVNode(factory.ark.svg, vue.mergeProps(api.value.circleProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ProgressCircle'
});

exports.ProgressCircle = ProgressCircle;
