'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');

const ProgressValueText = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = progressContext.useProgressContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.valueTextProps, attrs), {
    default: () => [slots.default?.() || api.value.valueAsString]
  });
}, {
  name: 'ProgressValueText'
});

exports.ProgressValueText = ProgressValueText;
