'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');

const ProgressCircleRange = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = progressContext.useProgressContext();
  return () => vue.createVNode(factory.ark.circle, vue.mergeProps(api.value.circleRangeProps, attrs), null);
}, {
  name: 'ProgressCircleRange'
});

exports.ProgressCircleRange = ProgressCircleRange;
