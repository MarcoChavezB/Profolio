import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { ProgressProvider } from './progress-context.mjs';
import { props, emits } from './progress.props.mjs';
import { useProgress } from './use-progress.mjs';

const ProgressRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useProgress(props);
  ProgressProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'ProgressRoot',
  props,
  emits
});

export { ProgressRoot };
