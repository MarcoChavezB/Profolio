'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');

const ProgressTrack = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = progressContext.useProgressContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.trackProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ProgressTrack'
});

exports.ProgressTrack = ProgressTrack;
