'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');

const ProgressRange = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = progressContext.useProgressContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rangeProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ProgressRange'
});

exports.ProgressRange = ProgressRange;
