import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useProgressContext } from './progress-context.mjs';

const ProgressCircleRange = /* @__PURE__ */ defineComponent((_, {
  attrs
}) => {
  const api = useProgressContext();
  return () => createVNode(ark.circle, mergeProps(api.value.circleRangeProps, attrs), null);
}, {
  name: 'ProgressCircleRange'
});

export { ProgressCircleRange };
