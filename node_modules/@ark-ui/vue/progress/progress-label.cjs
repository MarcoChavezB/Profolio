'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');

const ProgressLabel = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = progressContext.useProgressContext();
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ProgressLabel'
});

exports.ProgressLabel = ProgressLabel;
