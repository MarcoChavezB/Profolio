import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useProgressContext } from './progress-context.mjs';

const ProgressValueText = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useProgressContext();
  return () => createVNode(ark.span, mergeProps(api.value.valueTextProps, attrs), {
    default: () => [slots.default?.() || api.value.valueAsString]
  });
}, {
  name: 'ProgressValueText'
});

export { ProgressValueText };
