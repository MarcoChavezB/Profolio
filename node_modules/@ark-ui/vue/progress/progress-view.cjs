'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const progressContext = require('./progress-context.cjs');

const ProgressView = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = progressContext.useProgressContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.getViewProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ProgressView',
  props: {
    state: {
      type: String,
      default: 'indeterminate'
    }
  }
});

exports.ProgressView = ProgressView;
