import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useProgressContext } from './progress-context.mjs';

const ProgressCircleTrack = /* @__PURE__ */ defineComponent((_, {
  attrs
}) => {
  const api = useProgressContext();
  return () => createVNode(ark.circle, mergeProps(api.value.circleTrackProps, attrs), null);
}, {
  name: 'ProgressCircleTrack'
});

export { ProgressCircleTrack };
