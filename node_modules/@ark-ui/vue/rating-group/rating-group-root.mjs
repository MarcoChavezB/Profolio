import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { RatingGroupProvider } from './rating-group-context.mjs';
import { props, emits } from './rating-group.props.mjs';
import { useRatingGroup } from './use-rating-group.mjs';

const RatingGroupRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useRatingGroup(props, emit);
  RatingGroupProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'RatingGroupRoot',
  props,
  emits
});

export { RatingGroupRoot };
