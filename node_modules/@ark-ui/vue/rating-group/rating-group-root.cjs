'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const ratingGroupContext = require('./rating-group-context.cjs');
const ratingGroup_props = require('./rating-group.props.cjs');
const useRatingGroup = require('./use-rating-group.cjs');

const RatingGroupRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useRatingGroup.useRatingGroup(props, emit);
  ratingGroupContext.RatingGroupProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'RatingGroupRoot',
  props: ratingGroup_props.props,
  emits: ratingGroup_props.emits
});

exports.RatingGroupRoot = RatingGroupRoot;
