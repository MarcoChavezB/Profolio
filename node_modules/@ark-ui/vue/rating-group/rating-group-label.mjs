import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useRatingGroupContext } from './rating-group-context.mjs';

const RatingGroupLabel = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useRatingGroupContext();
  return () => createVNode(ark.label, mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'RatingGroupLabel'
});

export { RatingGroupLabel };
