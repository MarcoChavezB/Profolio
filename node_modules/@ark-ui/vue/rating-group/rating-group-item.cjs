'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const ratingGroupContext = require('./rating-group-context.cjs');
const ratingGroupItemContext = require('./rating-group-item-context.cjs');

const RatingGroupItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = ratingGroupContext.useRatingGroupContext();
  const itemState = vue.computed(() => api.value.getItemState(props));
  ratingGroupItemContext.RatingGroupItemProvider(vue.computed(() => props));
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(itemState.value)]
  });
}, {
  name: 'RatingGroupItem',
  props: {
    index: {
      type: Number,
      required: true
    }
  }
});

exports.RatingGroupItem = RatingGroupItem;
