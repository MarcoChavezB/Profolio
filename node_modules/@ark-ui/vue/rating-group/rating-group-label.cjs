'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const ratingGroupContext = require('./rating-group-context.cjs');

const RatingGroupLabel = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = ratingGroupContext.useRatingGroupContext();
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'RatingGroupLabel'
});

exports.RatingGroupLabel = RatingGroupLabel;
