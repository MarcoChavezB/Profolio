'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const avatarContext = require('./avatar-context.cjs');

const AvatarImage = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = avatarContext.useAvatarContext();
  return () => vue.createVNode(factory.ark.img, vue.mergeProps(api.value.imageProps, attrs), null);
}, {
  name: 'AvatarImage'
});

exports.AvatarImage = AvatarImage;
