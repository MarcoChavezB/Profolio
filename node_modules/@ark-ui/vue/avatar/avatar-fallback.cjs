'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const avatarContext = require('./avatar-context.cjs');

const AvatarFallback = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = avatarContext.useAvatarContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.fallbackProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'AvatarFallback'
});

exports.AvatarFallback = AvatarFallback;
