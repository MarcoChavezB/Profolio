import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { AvatarProvider } from './avatar-context.mjs';
import { props, emits } from './avatar.props.mjs';
import { useAvatar } from './use-avatar.mjs';

const AvatarRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useAvatar(props, emit);
  AvatarProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'AvatarRoot',
  props,
  emits
});

export { AvatarRoot };
