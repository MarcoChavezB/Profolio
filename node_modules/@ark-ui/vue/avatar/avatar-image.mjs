import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useAvatarContext } from './avatar-context.mjs';

const AvatarImage = /* @__PURE__ */ defineComponent((_, {
  attrs
}) => {
  const api = useAvatarContext();
  return () => createVNode(ark.img, mergeProps(api.value.imageProps, attrs), null);
}, {
  name: 'AvatarImage'
});

export { AvatarImage };
