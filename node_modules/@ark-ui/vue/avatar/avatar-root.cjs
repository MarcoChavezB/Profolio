'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const avatarContext = require('./avatar-context.cjs');
const avatar_props = require('./avatar.props.cjs');
const useAvatar = require('./use-avatar.cjs');

const AvatarRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useAvatar.useAvatar(props, emit);
  avatarContext.AvatarProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'AvatarRoot',
  props: avatar_props.props,
  emits: avatar_props.emits
});

exports.AvatarRoot = AvatarRoot;
