import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { usePresenceContext } from '../presence/presence-context.mjs';
import { props, emits } from '../presence/presence.props.mjs';
import { usePopoverContext } from './popover-context.mjs';

const PopoverContent = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = usePopoverContext();
  const presenceApi = usePresenceContext();
  return () => createVNode(Fragment, null, [presenceApi.value.isUnmounted ? null : createVNode(ark.div, mergeProps(api.value.contentProps, attrs), {
    default: () => [slots.default?.()]
  })]);
}, {
  name: 'PopoverContent',
  props,
  emits
});

export { PopoverContent };
