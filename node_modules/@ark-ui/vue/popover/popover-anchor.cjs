'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const popoverContext = require('./popover-context.cjs');

const PopoverAnchor = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = popoverContext.usePopoverContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.anchorProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PopoverAnchor'
});

exports.PopoverAnchor = PopoverAnchor;
