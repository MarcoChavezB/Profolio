import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { usePopoverContext } from './popover-context.mjs';

const PopoverCloseTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = usePopoverContext();
  return () => createVNode(ark.button, mergeProps(api.value.closeTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PopoverCloseTrigger'
});

export { PopoverCloseTrigger };
