'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const presenceContext = require('../presence/presence-context.cjs');
const presence_props = require('../presence/presence.props.cjs');
const popoverContext = require('./popover-context.cjs');

const PopoverContent = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = popoverContext.usePopoverContext();
  const presenceApi = presenceContext.usePresenceContext();
  return () => vue.createVNode(vue.Fragment, null, [presenceApi.value.isUnmounted ? null : vue.createVNode(factory.ark.div, vue.mergeProps(api.value.contentProps, attrs), {
    default: () => [slots.default?.()]
  })]);
}, {
  name: 'PopoverContent',
  props: presence_props.props,
  emits: presence_props.emits
});

exports.PopoverContent = PopoverContent;
