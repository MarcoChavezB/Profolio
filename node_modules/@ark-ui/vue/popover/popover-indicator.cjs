'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const popoverContext = require('./popover-context.cjs');

const PopoverIndicator = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = popoverContext.usePopoverContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.indicatorProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PopoverIndicator'
});

exports.PopoverIndicator = PopoverIndicator;
