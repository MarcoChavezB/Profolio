'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const popoverContext = require('./popover-context.cjs');

const PopoverCloseTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = popoverContext.usePopoverContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.closeTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PopoverCloseTrigger'
});

exports.PopoverCloseTrigger = PopoverCloseTrigger;
