'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');

const DatePickerInput = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = datePickerContext.useDatePickerContext();
  return () => vue.createVNode(factory.ark.input, vue.mergeProps(api.value.inputProps, attrs), null);
}, {
  name: 'DatePickerInput'
});

exports.DatePickerInput = DatePickerInput;
