import { defineComponent, createVNode, mergeProps, isVNode } from 'vue';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';

function _isSlot(s) {
  return typeof s === 'function' || Object.prototype.toString.call(s) === '[object Object]' && !isVNode(s);
}
const DatePickerMonthSelect = /* @__PURE__ */ defineComponent((_, {
  attrs
}) => {
  const api = useDatePickerContext();
  return () => {
    let _slot;
    return createVNode(ark.select, mergeProps(api.value.monthSelectProps, attrs), _isSlot(_slot = api.value.getMonths().map((month, i) => createVNode("option", {
      "key": i,
      "value": month.value
    }, [month.label]))) ? _slot : {
      default: () => [_slot]
    });
  };
}, {
  name: 'DatePickerMonthSelect'
});

export { DatePickerMonthSelect };
