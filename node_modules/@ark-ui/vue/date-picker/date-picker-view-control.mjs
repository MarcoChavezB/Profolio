import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';
import { useDatePickerViewContext } from './date-picker-view-context.mjs';

const DatePickerViewControl = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  const api = useDatePickerContext();
  const view = useDatePickerViewContext();
  return () => createVNode(ark.div, mergeProps(api.value.getViewControlProps(view), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerViewControl'
});

export { DatePickerViewControl };
