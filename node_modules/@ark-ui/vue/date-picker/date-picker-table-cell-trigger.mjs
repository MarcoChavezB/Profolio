import { defineComponent, computed, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';
import { useDatePickerTableCellContext } from './date-picker-table-cell-context.mjs';
import { useDatePickerViewContext } from './date-picker-view-context.mjs';

const DatePickerTableCellTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useDatePickerContext();
  const cell = useDatePickerTableCellContext();
  const view = useDatePickerViewContext();
  const triggerProps = computed(() => {
    return {
      day: api.value.getDayTableCellTriggerProps,
      month: api.value.getMonthTableCellTriggerProps,
      year: api.value.getYearTableCellTriggerProps
      // @ts-expect-error use filter guard
    }[view.view](cell);
  });
  return () => createVNode(ark.button, mergeProps(triggerProps.value, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerTableCellTrigger'
});

export { DatePickerTableCellTrigger };
