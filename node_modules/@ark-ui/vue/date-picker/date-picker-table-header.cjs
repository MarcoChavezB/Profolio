'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');
const datePickerTableContext = require('./date-picker-table-context.cjs');

const DatePickerTableHeader = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = datePickerContext.useDatePickerContext();
  const table = datePickerTableContext.useDatePickerTableContext();
  return () => vue.createVNode(factory.ark.th, vue.mergeProps(api.value.getTableHeaderProps(table), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerTableHeader'
});

exports.DatePickerTableHeader = DatePickerTableHeader;
