import * as datePicker from '@zag-js/date-picker';
import { type PropTypes } from '@zag-js/vue';
import { type ComputedRef } from 'vue';
import type { Optional } from '../types';
export interface UseDatePickerProps extends Optional<Omit<datePicker.Context, 'value' | 'focusedValue'>, 'id'> {
    /**
     * The focused date.
     */
    focusedValue?: datePicker.Context['focusedValue'];
    modelValue?: datePicker.Context['value'];
}
export interface UseDatePickerReturn extends ComputedRef<datePicker.Api<PropTypes>> {
}
export declare const useDatePicker: (props: UseDatePickerProps, emit: CallableFunction) => UseDatePickerReturn;
