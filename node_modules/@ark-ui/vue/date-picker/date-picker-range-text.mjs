import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';

const DatePickerRangeText = /* @__PURE__ */ defineComponent((_, {
  attrs
}) => {
  const api = useDatePickerContext();
  return () => createVNode(ark.div, mergeProps(api.value.rangeTextProps, attrs), {
    default: () => [api.value.visibleRangeText.start]
  });
}, {
  name: 'DatePickerRangeText'
});

export { DatePickerRangeText };
