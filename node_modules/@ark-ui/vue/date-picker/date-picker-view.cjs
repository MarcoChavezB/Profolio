'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const anatomy = require('@ark-ui/anatomy');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');
const datePickerViewContext = require('./date-picker-view-context.cjs');

const DatePickerView = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = datePickerContext.useDatePickerContext();
  const reactiveProps = vue.reactive(props);
  datePickerViewContext.DatePickerViewProvider(reactiveProps);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps({
    "hidden": api.value.view !== reactiveProps.view
  }, anatomy.datePickerAnatomy.build().view.attrs, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'DatePickerView',
  props: {
    view: {
      type: String,
      required: true
    }
  }
});

exports.DatePickerView = DatePickerView;
