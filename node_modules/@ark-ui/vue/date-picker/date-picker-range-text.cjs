'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');

const DatePickerRangeText = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = datePickerContext.useDatePickerContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rangeTextProps, attrs), {
    default: () => [api.value.visibleRangeText.start]
  });
}, {
  name: 'DatePickerRangeText'
});

exports.DatePickerRangeText = DatePickerRangeText;
