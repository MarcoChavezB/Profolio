'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');
const datePickerTableContext = require('./date-picker-table-context.cjs');

const DatePickerTableHead = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = datePickerContext.useDatePickerContext();
  const table = datePickerTableContext.useDatePickerTableContext();
  return () => vue.createVNode(factory.ark.thead, vue.mergeProps(api.value.getTableHeadProps(table), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerTableHead'
});

exports.DatePickerTableHead = DatePickerTableHead;
