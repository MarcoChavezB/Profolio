'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');

const DatePickerControl = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = datePickerContext.useDatePickerContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerControl'
});

exports.DatePickerControl = DatePickerControl;
