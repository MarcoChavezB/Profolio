'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');

const DatePickerTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = datePickerContext.useDatePickerContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.triggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerTrigger'
});

exports.DatePickerTrigger = DatePickerTrigger;
