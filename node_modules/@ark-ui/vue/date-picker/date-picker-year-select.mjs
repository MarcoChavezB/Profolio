import { defineComponent, createVNode, mergeProps, isVNode } from 'vue';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';

function _isSlot(s) {
  return typeof s === 'function' || Object.prototype.toString.call(s) === '[object Object]' && !isVNode(s);
}
const DatePickerYearSelect = /* @__PURE__ */ defineComponent((_, {
  attrs
}) => {
  const api = useDatePickerContext();
  return () => {
    let _slot;
    return createVNode(ark.select, mergeProps(api.value.yearSelectProps, attrs), _isSlot(_slot = getYearsRange({
      from: 1_000,
      to: 4_000
    }).map((year, i) => createVNode("option", {
      "key": i,
      "value": year
    }, [year]))) ? _slot : {
      default: () => [_slot]
    });
  };
}, {
  name: 'DatePickerYearSelect'
});
function getYearsRange(range) {
  const years = [];
  for (let year = range.from; year <= range.to; year += 1) {
    years.push(year);
  }
  return years;
}

export { DatePickerYearSelect };
