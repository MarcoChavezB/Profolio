'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');
const datePickerTableCellContext = require('./date-picker-table-cell-context.cjs');
const datePickerViewContext = require('./date-picker-view-context.cjs');

const DatePickerTableCellTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = datePickerContext.useDatePickerContext();
  const cell = datePickerTableCellContext.useDatePickerTableCellContext();
  const view = datePickerViewContext.useDatePickerViewContext();
  const triggerProps = vue.computed(() => {
    return {
      day: api.value.getDayTableCellTriggerProps,
      month: api.value.getMonthTableCellTriggerProps,
      year: api.value.getYearTableCellTriggerProps
      // @ts-expect-error use filter guard
    }[view.view](cell);
  });
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(triggerProps.value, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerTableCellTrigger'
});

exports.DatePickerTableCellTrigger = DatePickerTableCellTrigger;
