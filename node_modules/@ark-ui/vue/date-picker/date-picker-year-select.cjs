'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');

function _isSlot(s) {
  return typeof s === 'function' || Object.prototype.toString.call(s) === '[object Object]' && !vue.isVNode(s);
}
const DatePickerYearSelect = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = datePickerContext.useDatePickerContext();
  return () => {
    let _slot;
    return vue.createVNode(factory.ark.select, vue.mergeProps(api.value.yearSelectProps, attrs), _isSlot(_slot = getYearsRange({
      from: 1_000,
      to: 4_000
    }).map((year, i) => vue.createVNode("option", {
      "key": i,
      "value": year
    }, [year]))) ? _slot : {
      default: () => [_slot]
    });
  };
}, {
  name: 'DatePickerYearSelect'
});
function getYearsRange(range) {
  const years = [];
  for (let year = range.from; year <= range.to; year += 1) {
    years.push(year);
  }
  return years;
}

exports.DatePickerYearSelect = DatePickerYearSelect;
