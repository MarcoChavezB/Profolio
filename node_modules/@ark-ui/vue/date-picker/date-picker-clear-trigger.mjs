import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';

const DatePickerClearTrigger = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  const api = useDatePickerContext();
  return () => createVNode(ark.button, mergeProps(api.value.clearTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerClearTrigger'
});

export { DatePickerClearTrigger };
