'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');

const DatePickerLabel = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = datePickerContext.useDatePickerContext();
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerLabel'
});

exports.DatePickerLabel = DatePickerLabel;
