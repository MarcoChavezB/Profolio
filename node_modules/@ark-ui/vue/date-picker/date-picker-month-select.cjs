'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');

function _isSlot(s) {
  return typeof s === 'function' || Object.prototype.toString.call(s) === '[object Object]' && !vue.isVNode(s);
}
const DatePickerMonthSelect = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = datePickerContext.useDatePickerContext();
  return () => {
    let _slot;
    return vue.createVNode(factory.ark.select, vue.mergeProps(api.value.monthSelectProps, attrs), _isSlot(_slot = api.value.getMonths().map((month, i) => vue.createVNode("option", {
      "key": i,
      "value": month.value
    }, [month.label]))) ? _slot : {
      default: () => [_slot]
    });
  };
}, {
  name: 'DatePickerMonthSelect'
});

exports.DatePickerMonthSelect = DatePickerMonthSelect;
