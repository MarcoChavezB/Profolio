import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';
import { useDatePickerTableContext } from './date-picker-table-context.mjs';

const DatePickerTableHeader = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  const api = useDatePickerContext();
  const table = useDatePickerTableContext();
  return () => createVNode(ark.th, mergeProps(api.value.getTableHeaderProps(table), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DatePickerTableHeader'
});

export { DatePickerTableHeader };
