'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const checkboxContext = require('./checkbox-context.cjs');

const CheckboxIndicator = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = checkboxContext.useCheckboxContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.indicatorProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CheckboxIndicator'
});

exports.CheckboxIndicator = CheckboxIndicator;
