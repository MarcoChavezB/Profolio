import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { CheckboxProvider } from './checkbox-context.mjs';
import { props, emits } from './checkbox.props.mjs';
import { useCheckbox } from './use-checkbox.mjs';

const CheckboxRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useCheckbox(props, emit);
  CheckboxProvider(api);
  return () => createVNode(ark.label, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'CheckboxRoot',
  props,
  emits
});

export { CheckboxRoot };
