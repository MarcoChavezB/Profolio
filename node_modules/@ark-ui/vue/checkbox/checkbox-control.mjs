import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useCheckboxContext } from './checkbox-context.mjs';

const CheckboxControl = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  const api = useCheckboxContext();
  return () => createVNode(Fragment, null, [createVNode(ark.div, mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  }), createVNode("input", api.value.hiddenInputProps, null)]);
}, {
  name: 'CheckboxControl'
});

export { CheckboxControl };
