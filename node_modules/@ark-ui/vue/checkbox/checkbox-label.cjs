'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const checkboxContext = require('./checkbox-context.cjs');

const CheckboxLabel = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = checkboxContext.useCheckboxContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CheckboxLabel'
});

exports.CheckboxLabel = CheckboxLabel;
