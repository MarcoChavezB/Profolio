'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const checkbox = require('@zag-js/checkbox');
const vue$1 = require('@zag-js/vue');
const vue = require('vue');
const environmentContext = require('../environment/environment-context.cjs');
const utils = require('../utils.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const checkbox__namespace = /*#__PURE__*/_interopNamespaceDefault(checkbox);

const useCheckbox = (props, emit) => {
  const context = vue.ref(props);
  const getRootNode = environmentContext.useEnvironmentContext();
  const [state, send] = vue$1.useMachine(
    checkbox__namespace.machine({
      ...context.value,
      id: context.value.id ?? utils.useId().value,
      getRootNode,
      checked: props.checked ?? props.defaultChecked,
      onCheckedChange(details) {
        emit("checked-change", details);
        emit("update:checked", details.checked);
      }
    }),
    { context }
  );
  return vue.computed(() => checkbox__namespace.connect(state.value, send, vue$1.normalizeProps));
};

exports.useCheckbox = useCheckbox;
