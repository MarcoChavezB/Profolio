'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const checkboxContext = require('./checkbox-context.cjs');

const CheckboxControl = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = checkboxContext.useCheckboxContext();
  return () => vue.createVNode(vue.Fragment, null, [vue.createVNode(factory.ark.div, vue.mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  }), vue.createVNode("input", api.value.hiddenInputProps, null)]);
}, {
  name: 'CheckboxControl'
});

exports.CheckboxControl = CheckboxControl;
