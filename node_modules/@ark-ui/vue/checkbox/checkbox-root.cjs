'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const checkboxContext = require('./checkbox-context.cjs');
const checkbox_props = require('./checkbox.props.cjs');
const useCheckbox = require('./use-checkbox.cjs');

const CheckboxRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useCheckbox.useCheckbox(props, emit);
  checkboxContext.CheckboxProvider(api);
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'CheckboxRoot',
  props: checkbox_props.props,
  emits: checkbox_props.emits
});

exports.CheckboxRoot = CheckboxRoot;
