'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const menuContext = require('./menu-context.cjs');

const MenuContextTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = menuContext.useMenuContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.contextTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'MenuContextTrigger'
});

exports.MenuContextTrigger = MenuContextTrigger;
