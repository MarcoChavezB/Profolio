import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useMenuTriggerItemContext } from './menu-context.mjs';

const MenuTriggerItem = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const triggerItemProps = useMenuTriggerItemContext();
  return () => createVNode(ark.div, mergeProps(triggerItemProps.value, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'MenuTriggerItem'
});

export { MenuTriggerItem };
