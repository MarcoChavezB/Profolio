'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const menuContext = require('./menu-context.cjs');

const MenuItemGroup = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = menuContext.useMenuContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemGroupProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'MenuItemGroup',
  props: {
    id: {
      type: String,
      required: true
    }
  }
});

exports.MenuItemGroup = MenuItemGroup;
