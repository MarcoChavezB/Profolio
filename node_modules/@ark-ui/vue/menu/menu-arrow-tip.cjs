'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const menuContext = require('./menu-context.cjs');

const MenuArrowTip = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = menuContext.useMenuContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.arrowTipProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'MenuArrowTip'
});

exports.MenuArrowTip = MenuArrowTip;
