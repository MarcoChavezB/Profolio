import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useMenuContext } from './menu-context.mjs';

const MenuItemGroupLabel = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useMenuContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemGroupLabelProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'MenuItemGroupLabel',
  props: {
    htmlFor: {
      type: String,
      required: true
    }
  }
});

export { MenuItemGroupLabel };
