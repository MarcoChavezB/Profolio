import type { CollectionOptions } from '@zag-js/combobox';
import * as combobox from '@zag-js/combobox';
import { type PropTypes } from '@zag-js/vue';
import { type ComputedRef } from 'vue';
import type { CollectionItem, Optional } from '../types';
export interface UseComboboxProps<T extends CollectionItem> extends CollectionOptions<T>, Omit<Optional<combobox.Context<T>, 'id'>, 'collection'> {
    modelValue?: combobox.Context<T>['value'];
}
export interface UseComboboxReturn<T extends CollectionItem> extends ComputedRef<combobox.Api<PropTypes, T>> {
}
export declare const useCombobox: <T extends CollectionItem>(props: UseComboboxProps<T>, emit: CallableFunction) => UseComboboxReturn<T>;
