'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const comboboxContext = require('./combobox-context.cjs');
const comboboxItemContext = require('./combobox-item-context.cjs');

const ComboboxItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = comboboxContext.useComboboxContext();
  comboboxItemContext.ComboboxItemProvider(vue.computed(() => props));
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemState(props))]
  });
}, {
  name: 'ComboboxItem',
  props: {
    item: {
      type: Object,
      required: true
    }
  }
});

exports.ComboboxItem = ComboboxItem;
