'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const comboboxContext = require('./combobox-context.cjs');

const ComboboxItemGroup = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = comboboxContext.useComboboxContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemGroupProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ComboboxItemGroup',
  props: {
    id: {
      type: String,
      required: true
    }
  }
});

exports.ComboboxItemGroup = ComboboxItemGroup;
