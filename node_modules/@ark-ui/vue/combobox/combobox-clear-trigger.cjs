'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const comboboxContext = require('./combobox-context.cjs');

const ComboboxClearTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = comboboxContext.useComboboxContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.clearTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {});

exports.ComboboxClearTrigger = ComboboxClearTrigger;
