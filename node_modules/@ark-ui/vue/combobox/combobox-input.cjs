'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const comboboxContext = require('./combobox-context.cjs');

const ComboboxInput = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = comboboxContext.useComboboxContext();
  const inputProps = vue.computed(() => ({
    ...api.value.inputProps,
    modelValue: api.value.inputValue
  }));
  return () => vue.createVNode(factory.ark.input, vue.mergeProps(inputProps.value, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ComboboxInput'
});

exports.ComboboxInput = ComboboxInput;
