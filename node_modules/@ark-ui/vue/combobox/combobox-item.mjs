import { defineComponent, computed, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useComboboxContext } from './combobox-context.mjs';
import { ComboboxItemProvider } from './combobox-item-context.mjs';

const ComboboxItem = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useComboboxContext();
  ComboboxItemProvider(computed(() => props));
  return () => createVNode(ark.div, mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemState(props))]
  });
}, {
  name: 'ComboboxItem',
  props: {
    item: {
      type: Object,
      required: true
    }
  }
});

export { ComboboxItem };
