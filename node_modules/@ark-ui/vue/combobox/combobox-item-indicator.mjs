import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useComboboxContext } from './combobox-context.mjs';
import { useComboboxItemContext } from './combobox-item-context.mjs';

const ComboboxItemIndicator = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useComboboxContext();
  const itemProps = useComboboxItemContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemIndicatorProps(itemProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ComboboxItemIndicator'
});

export { ComboboxItemIndicator };
