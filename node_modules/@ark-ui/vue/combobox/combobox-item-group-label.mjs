import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useComboboxContext } from './combobox-context.mjs';

const ComboboxItemGroupLabel = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useComboboxContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemGroupLabelProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ComboboxItemGroupLabel',
  props: {
    htmlFor: {
      type: String,
      required: true
    }
  }
});

export { ComboboxItemGroupLabel };
