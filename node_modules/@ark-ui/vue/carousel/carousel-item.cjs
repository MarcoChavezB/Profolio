'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const carouselContext = require('./carousel-context.cjs');

const CarouselItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = carouselContext.useCarouselContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselItem',
  props: {
    index: {
      type: Number,
      required: true
    }
  }
});

exports.CarouselItem = CarouselItem;
