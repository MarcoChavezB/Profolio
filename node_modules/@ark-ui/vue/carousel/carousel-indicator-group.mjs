import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useCarouselContext } from './carousel-context.mjs';

const CarouselIndicatorGroup = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useCarouselContext();
  return () => createVNode(ark.div, mergeProps(api.value.indicatorGroupProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselIndicatorGroup'
});

export { CarouselIndicatorGroup };
