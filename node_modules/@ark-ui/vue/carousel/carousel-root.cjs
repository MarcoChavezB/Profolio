'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const carouselContext = require('./carousel-context.cjs');
const carousel_props = require('./carousel.props.cjs');
const useCarousel = require('./use-carousel.cjs');

const CarouselRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useCarousel.useCarousel(props, emit);
  carouselContext.CarouselProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'CarouselRoot',
  props: carousel_props.props,
  emits: carousel_props.emits
});

exports.CarouselRoot = CarouselRoot;
