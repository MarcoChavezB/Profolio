import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { CarouselProvider } from './carousel-context.mjs';
import { props, emits } from './carousel.props.mjs';
import { useCarousel } from './use-carousel.mjs';

const CarouselRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useCarousel(props, emit);
  CarouselProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'CarouselRoot',
  props,
  emits
});

export { CarouselRoot };
