'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const carouselContext = require('./carousel-context.cjs');

const CarouselNextTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = carouselContext.useCarouselContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.nextTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselNextTrigger'
});

exports.CarouselNextTrigger = CarouselNextTrigger;
