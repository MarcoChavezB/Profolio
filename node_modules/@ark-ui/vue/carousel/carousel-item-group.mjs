import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useCarouselContext } from './carousel-context.mjs';

const CarouselItemGroup = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useCarouselContext();
  return () => createVNode(ark.div, mergeProps(api.value.itemGroupProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselItemGroup'
});

export { CarouselItemGroup };
