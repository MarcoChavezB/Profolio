'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const carouselContext = require('./carousel-context.cjs');

const CarouselIndicator = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = carouselContext.useCarouselContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.getIndicatorProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselIndicator',
  props: {
    index: {
      type: Number,
      required: true
    },
    readOnly: {
      type: Boolean,
      default: false
    }
  }
});

exports.CarouselIndicator = CarouselIndicator;
