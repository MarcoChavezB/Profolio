import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useCarouselContext } from './carousel-context.mjs';

const CarouselNextTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useCarouselContext();
  return () => createVNode(ark.button, mergeProps(api.value.nextTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselNextTrigger'
});

export { CarouselNextTrigger };
