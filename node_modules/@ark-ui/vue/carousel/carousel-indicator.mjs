import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useCarouselContext } from './carousel-context.mjs';

const CarouselIndicator = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useCarouselContext();
  return () => createVNode(ark.button, mergeProps(api.value.getIndicatorProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselIndicator',
  props: {
    index: {
      type: Number,
      required: true
    },
    readOnly: {
      type: Boolean,
      default: false
    }
  }
});

export { CarouselIndicator };
