'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const carouselContext = require('./carousel-context.cjs');

const CarouselPrevTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = carouselContext.useCarouselContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.prevTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselPrevTrigger'
});

exports.CarouselPrevTrigger = CarouselPrevTrigger;
