import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useCarouselContext } from './carousel-context.mjs';

const CarouselPrevTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useCarouselContext();
  return () => createVNode(ark.button, mergeProps(api.value.prevTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselPrevTrigger'
});

export { CarouselPrevTrigger };
