'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const carouselContext = require('./carousel-context.cjs');

const CarouselItemGroup = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = carouselContext.useCarouselContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.itemGroupProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselItemGroup'
});

exports.CarouselItemGroup = CarouselItemGroup;
