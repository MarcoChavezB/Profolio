'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const carouselContext = require('./carousel-context.cjs');

const CarouselIndicatorGroup = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = carouselContext.useCarouselContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.indicatorGroupProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'CarouselIndicatorGroup'
});

exports.CarouselIndicatorGroup = CarouselIndicatorGroup;
