import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { usePinInputContext } from './pin-input-context.mjs';

const PinInputControl = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = usePinInputContext();
  return () => createVNode(ark.div, mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PinInputControl'
});

export { PinInputControl };
