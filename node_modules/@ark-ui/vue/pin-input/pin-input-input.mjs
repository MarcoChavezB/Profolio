import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { usePinInputContext } from './pin-input-context.mjs';

const PinInputInput = /* @__PURE__ */ defineComponent((props, {
  attrs
}) => {
  const api = usePinInputContext();
  return () => createVNode(ark.input, mergeProps(api.value.getInputProps({
    index: props.index
  }), attrs), null);
}, {
  name: 'PinInputInput',
  props: {
    index: {
      type: Number,
      required: true
    }
  }
});

export { PinInputInput };
