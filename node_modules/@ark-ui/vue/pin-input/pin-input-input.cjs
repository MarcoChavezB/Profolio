'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const pinInputContext = require('./pin-input-context.cjs');

const PinInputInput = /* @__PURE__ */ vue.defineComponent((props, {
  attrs
}) => {
  const api = pinInputContext.usePinInputContext();
  return () => vue.createVNode(factory.ark.input, vue.mergeProps(api.value.getInputProps({
    index: props.index
  }), attrs), null);
}, {
  name: 'PinInputInput',
  props: {
    index: {
      type: Number,
      required: true
    }
  }
});

exports.PinInputInput = PinInputInput;
