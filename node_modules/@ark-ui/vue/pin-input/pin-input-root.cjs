'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const pinInputContext = require('./pin-input-context.cjs');
const pinInput_props = require('./pin-input.props.cjs');
const usePinInput = require('./use-pin-input.cjs');

const PinInputRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = usePinInput.usePinInput(props, emit);
  pinInputContext.PinInputProvider(api);
  return () => vue.createVNode(vue.Fragment, null, [vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  }), vue.createVNode("input", api.value.hiddenInputProps, null)]);
}, {
  name: 'PinInputRoot',
  props: pinInput_props.props,
  emits: pinInput_props.emits
});

exports.PinInputRoot = PinInputRoot;
