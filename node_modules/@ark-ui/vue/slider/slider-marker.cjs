'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const sliderContext = require('./slider-context.cjs');

const SliderMarker = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = sliderContext.useSliderContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.getMarkerProps({
    value: props.value
  }), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SliderMarker',
  props: {
    value: {
      type: Number,
      required: true
    }
  }
});

exports.SliderMarker = SliderMarker;
