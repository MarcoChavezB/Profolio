import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSliderContext } from './slider-context.mjs';

const SliderValueText = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useSliderContext();
  return () => createVNode(ark.span, mergeProps(api.value.valueTextProps, attrs), {
    default: () => [slots.default?.() || api.value.value.join(', ')]
  });
}, {
  name: 'SliderValueText'
});

export { SliderValueText };
