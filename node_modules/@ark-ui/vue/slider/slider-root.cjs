'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const sliderContext = require('./slider-context.cjs');
const slider_props = require('./slider.props.cjs');
const useSlider = require('./use-slider.cjs');

const SliderRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useSlider.useSlider(props, emit);
  sliderContext.SliderProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'SliderRoot',
  props: slider_props.props,
  emits: slider_props.emits
});

exports.SliderRoot = SliderRoot;
