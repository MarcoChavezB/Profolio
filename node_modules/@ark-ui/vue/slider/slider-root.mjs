import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { SliderProvider } from './slider-context.mjs';
import { props, emits } from './slider.props.mjs';
import { useSlider } from './use-slider.mjs';

const SliderRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useSlider(props, emit);
  SliderProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'SliderRoot',
  props,
  emits
});

export { SliderRoot };
