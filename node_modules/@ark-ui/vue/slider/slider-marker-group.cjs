'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const sliderContext = require('./slider-context.cjs');

const SliderMarkerGroup = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = sliderContext.useSliderContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.markerGroupProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SliderMarkerGroup'
});

exports.SliderMarkerGroup = SliderMarkerGroup;
