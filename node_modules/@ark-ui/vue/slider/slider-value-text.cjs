'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const sliderContext = require('./slider-context.cjs');

const SliderValueText = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = sliderContext.useSliderContext();
  return () => vue.createVNode(factory.ark.span, vue.mergeProps(api.value.valueTextProps, attrs), {
    default: () => [slots.default?.() || api.value.value.join(', ')]
  });
}, {
  name: 'SliderValueText'
});

exports.SliderValueText = SliderValueText;
