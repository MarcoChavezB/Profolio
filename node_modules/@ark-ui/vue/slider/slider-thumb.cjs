'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const sliderContext = require('./slider-context.cjs');

const SliderThumb = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = sliderContext.useSliderContext();
  return () => vue.createVNode(vue.Fragment, null, [vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getThumbProps(props), attrs), {
    default: () => [slots.default?.()]
  }), vue.createVNode("input", api.value.getHiddenInputProps(props), null)]);
}, {
  name: 'SliderThumb',
  props: {
    index: {
      type: Number,
      required: true
    }
  }
});

exports.SliderThumb = SliderThumb;
