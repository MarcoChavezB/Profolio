import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSliderContext } from './slider-context.mjs';

const SliderThumb = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useSliderContext();
  return () => createVNode(Fragment, null, [createVNode(ark.div, mergeProps(api.value.getThumbProps(props), attrs), {
    default: () => [slots.default?.()]
  }), createVNode("input", api.value.getHiddenInputProps(props), null)]);
}, {
  name: 'SliderThumb',
  props: {
    index: {
      type: Number,
      required: true
    }
  }
});

export { SliderThumb };
