'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const sliderContext = require('./slider-context.cjs');

const SliderTrack = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = sliderContext.useSliderContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.trackProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SliderTrack'
});

exports.SliderTrack = SliderTrack;
