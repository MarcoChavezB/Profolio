import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSliderContext } from './slider-context.mjs';

const SliderMarker = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useSliderContext();
  return () => createVNode(ark.span, mergeProps(api.value.getMarkerProps({
    value: props.value
  }), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SliderMarker',
  props: {
    value: {
      type: Number,
      required: true
    }
  }
});

export { SliderMarker };
