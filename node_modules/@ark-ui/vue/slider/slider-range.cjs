'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const sliderContext = require('./slider-context.cjs');

const SliderRange = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = sliderContext.useSliderContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rangeProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SliderRange'
});

exports.SliderRange = SliderRange;
