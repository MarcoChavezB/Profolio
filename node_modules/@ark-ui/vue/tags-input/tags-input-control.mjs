import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTagsInputContext } from './tags-input-context.mjs';

const TagsInputControl = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useTagsInputContext();
  return () => createVNode(ark.div, mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TagsInputControl'
});

export { TagsInputControl };
