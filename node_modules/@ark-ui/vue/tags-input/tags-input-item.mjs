import { defineComponent, computed, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTagsInputContext } from './tags-input-context.mjs';
import { TagsInputItemProvider } from './tags-input-item-context.mjs';

const TagsInputItem = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useTagsInputContext();
  TagsInputItemProvider(computed(() => props));
  return () => createVNode(ark.div, mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemState(props))]
  });
}, {
  name: 'TagsInputItem',
  props: {
    index: {
      type: [String, Number],
      required: true
    },
    value: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

export { TagsInputItem };
