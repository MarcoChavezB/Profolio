import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTagsInputContext } from './tags-input-context.mjs';
import { useTagsInputItemContext } from './tags-input-item-context.mjs';

const TagsInputItemInput = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useTagsInputContext();
  const itemProps = useTagsInputItemContext();
  return () => createVNode(ark.input, mergeProps(api.value.getItemInputProps(itemProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TagsInputItemInput'
});

export { TagsInputItemInput };
