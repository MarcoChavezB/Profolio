'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const tagsInputContext = require('./tags-input-context.cjs');

const TagsInputInput = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = tagsInputContext.useTagsInputContext();
  const inputProps = vue.computed(() => ({
    ...api.value.inputProps,
    modelValue: api.value.inputValue
  }));
  return () => vue.createVNode(factory.ark.input, vue.mergeProps(inputProps.value, attrs), null);
}, {
  name: 'TagsInputInput'
});

exports.TagsInputInput = TagsInputInput;
