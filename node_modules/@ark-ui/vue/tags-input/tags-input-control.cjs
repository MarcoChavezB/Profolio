'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const tagsInputContext = require('./tags-input-context.cjs');

const TagsInputControl = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = tagsInputContext.useTagsInputContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.controlProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TagsInputControl'
});

exports.TagsInputControl = TagsInputControl;
