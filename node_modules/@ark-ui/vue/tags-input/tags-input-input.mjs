import { defineComponent, computed, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTagsInputContext } from './tags-input-context.mjs';

const TagsInputInput = /* @__PURE__ */ defineComponent((_, {
  attrs
}) => {
  const api = useTagsInputContext();
  const inputProps = computed(() => ({
    ...api.value.inputProps,
    modelValue: api.value.inputValue
  }));
  return () => createVNode(ark.input, mergeProps(inputProps.value, attrs), null);
}, {
  name: 'TagsInputInput'
});

export { TagsInputInput };
