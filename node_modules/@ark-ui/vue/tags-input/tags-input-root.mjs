import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { TagsInputProvider } from './tags-input-context.mjs';
import { props, emits } from './tags-input.props.mjs';
import { useTagsInput } from './use-tags-input.mjs';

const TagsInputRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useTagsInput(props, emit);
  TagsInputProvider(api);
  return () => createVNode(Fragment, null, [createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  }), createVNode("input", api.value.hiddenInputProps, null)]);
}, {
  name: 'TagsInputRoot',
  props,
  emits
});

export { TagsInputRoot };
