'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const tagsInputContext = require('./tags-input-context.cjs');
const tagsInputItemContext = require('./tags-input-item-context.cjs');

const TagsInputItemPreview = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = tagsInputContext.useTagsInputContext();
  const itemProps = tagsInputItemContext.useTagsInputItemContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemPreviewProps(itemProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TagsInputItemPreview'
});

exports.TagsInputItemPreview = TagsInputItemPreview;
