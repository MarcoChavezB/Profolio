'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const tagsInputContext = require('./tags-input-context.cjs');
const tagsInputItemContext = require('./tags-input-item-context.cjs');

const TagsInputItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = tagsInputContext.useTagsInputContext();
  tagsInputItemContext.TagsInputItemProvider(vue.computed(() => props));
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemState(props))]
  });
}, {
  name: 'TagsInputItem',
  props: {
    index: {
      type: [String, Number],
      required: true
    },
    value: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

exports.TagsInputItem = TagsInputItem;
