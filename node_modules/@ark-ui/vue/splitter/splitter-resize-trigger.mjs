import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSplitterContext } from './splitter-context.mjs';

const SplitterResizeTrigger = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useSplitterContext();
  return () => createVNode(ark.button, mergeProps(api.value.getResizeTriggerProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SplitterResizeTrigger',
  props: {
    id: {
      type: String,
      required: true
    },
    step: {
      type: Number
    },
    disabled: {
      type: Boolean
    }
  }
});

export { SplitterResizeTrigger };
