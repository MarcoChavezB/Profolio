'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const splitterContext = require('./splitter-context.cjs');

const SplitterResizeTrigger = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = splitterContext.useSplitterContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.getResizeTriggerProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SplitterResizeTrigger',
  props: {
    id: {
      type: String,
      required: true
    },
    step: {
      type: Number
    },
    disabled: {
      type: Boolean
    }
  }
});

exports.SplitterResizeTrigger = SplitterResizeTrigger;
