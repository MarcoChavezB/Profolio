'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const splitterContext = require('./splitter-context.cjs');

const SplitterPanel = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = splitterContext.useSplitterContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getPanelProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SplitterPanel',
  props: {
    id: {
      type: [String, Number],
      required: true
    },
    snapSize: Number
  }
});

exports.SplitterPanel = SplitterPanel;
