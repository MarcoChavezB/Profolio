'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const splitter = require('@zag-js/splitter');
const vue$1 = require('@zag-js/vue');
const vue = require('vue');
const environmentContext = require('../environment/environment-context.cjs');
const utils = require('../utils.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const splitter__namespace = /*#__PURE__*/_interopNamespaceDefault(splitter);

const useSplitter = (props, emit) => {
  const context = vue.ref(props);
  const getRootNode = environmentContext.useEnvironmentContext();
  const [state, send] = vue$1.useMachine(
    splitter__namespace.machine({
      ...context.value,
      id: context.value.id ?? utils.useId().value,
      getRootNode,
      onSizeChange: (details) => {
        emit("size-change", details);
      },
      onSizeChangeStart: (details) => {
        emit("size-change-start", details);
      },
      onSizeChangeEnd(details) {
        emit("size-change-end", details);
      }
    }),
    { context }
  );
  return vue.computed(() => splitter__namespace.connect(state.value, send, vue$1.normalizeProps));
};

exports.useSplitter = useSplitter;
