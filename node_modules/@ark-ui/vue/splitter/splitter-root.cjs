'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const splitterContext = require('./splitter-context.cjs');
const splitter_props = require('./splitter.props.cjs');
const useSplitter = require('./use-splitter.cjs');

const SplitterRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useSplitter.useSplitter(props, emit);
  splitterContext.SplitterProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'SplitterRoot',
  props: splitter_props.props,
  emits: splitter_props.emits
});

exports.SplitterRoot = SplitterRoot;
