import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { SplitterProvider } from './splitter-context.mjs';
import { props, emits } from './splitter.props.mjs';
import { useSplitter } from './use-splitter.mjs';

const SplitterRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useSplitter(props, emit);
  SplitterProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'SplitterRoot',
  props,
  emits
});

export { SplitterRoot };
