import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useSplitterContext } from './splitter-context.mjs';

const SplitterPanel = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useSplitterContext();
  return () => createVNode(ark.div, mergeProps(api.value.getPanelProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SplitterPanel',
  props: {
    id: {
      type: [String, Number],
      required: true
    },
    snapSize: Number
  }
});

export { SplitterPanel };
