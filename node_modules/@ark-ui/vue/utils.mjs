import { ref, computed } from 'vue';

let _id = 0;
const genId = () => ++_id;
const useId = (id, prefix) => {
  const initialId = id || genId();
  const uid = ref(initialId);
  return computed(() => {
    const __id__ = uid.value !== null ? uid.value.toString() : void 0;
    return prefix ? `${prefix}-${__id__}` : __id__;
  });
};
const declareEmits = (arr) => arr;

export { declareEmits, useId };
