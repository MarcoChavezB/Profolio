'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const hoverCardContext = require('./hover-card-context.cjs');

const HoverCardArrowTip = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = hoverCardContext.useHoverCardContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.arrowTipProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'HoverCardArrowTip'
});

exports.HoverCardArrowTip = HoverCardArrowTip;
