'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const hoverCard = require('@zag-js/hover-card');
const vue$1 = require('@zag-js/vue');
const vue = require('vue');
const environmentContext = require('../environment/environment-context.cjs');
const utils = require('../utils.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const hoverCard__namespace = /*#__PURE__*/_interopNamespaceDefault(hoverCard);

const useHoverCard = (props, emit) => {
  const context = vue.ref(props);
  const getRootNode = environmentContext.useEnvironmentContext();
  const [state, send] = vue$1.useMachine(
    hoverCard__namespace.machine({
      ...context.value,
      id: context.value.id ?? utils.useId().value,
      open: props.open ?? props.defaultOpen,
      getRootNode,
      onOpenChange: (details) => {
        emit("open-change", details);
        emit("update:open", details.open);
      }
    }),
    { context }
  );
  return vue.computed(() => hoverCard__namespace.connect(state.value, send, vue$1.normalizeProps));
};

exports.useHoverCard = useHoverCard;
