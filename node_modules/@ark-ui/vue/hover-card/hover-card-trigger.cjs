'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const hoverCardContext = require('./hover-card-context.cjs');

const HoverCardTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = hoverCardContext.useHoverCardContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.triggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'HoverCardTrigger'
});

exports.HoverCardTrigger = HoverCardTrigger;
