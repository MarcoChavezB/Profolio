import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useHoverCardContext } from './hover-card-context.mjs';

const HoverCardArrowTip = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useHoverCardContext();
  return () => createVNode(ark.div, mergeProps(api.value.arrowTipProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'HoverCardArrowTip'
});

export { HoverCardArrowTip };
