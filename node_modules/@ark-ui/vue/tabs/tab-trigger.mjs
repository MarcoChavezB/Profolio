import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useTabsContext } from './tabs-context.mjs';

const TabTrigger = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useTabsContext();
  return () => createVNode(ark.button, mergeProps(api.value.getTriggerProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TabTrigger',
  props: {
    value: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

export { TabTrigger };
