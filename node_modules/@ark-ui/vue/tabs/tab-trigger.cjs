'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const tabsContext = require('./tabs-context.cjs');

const TabTrigger = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = tabsContext.useTabsContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.getTriggerProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'TabTrigger',
  props: {
    value: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

exports.TabTrigger = TabTrigger;
