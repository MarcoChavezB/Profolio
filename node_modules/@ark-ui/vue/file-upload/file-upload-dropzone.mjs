import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useFileUploadContext } from './file-upload-context.mjs';

const FileUploadDropzone = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  const api = useFileUploadContext();
  return () => createVNode(Fragment, null, [createVNode(ark.div, mergeProps(api.value.dropzoneProps, attrs), {
    default: () => [slots.default?.()]
  }), createVNode("input", api.value.hiddenInputProps, null)]);
}, {
  name: 'FileUploadDropzone'
});

export { FileUploadDropzone };
