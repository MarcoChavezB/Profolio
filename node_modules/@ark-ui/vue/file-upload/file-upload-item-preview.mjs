import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useFileUploadContext } from './file-upload-context.mjs';
import { useFileUploadItemContext } from './file-upload-item-context.mjs';

const FileUploadItemPreview = /* @__PURE__ */ defineComponent((props, {
  attrs,
  slots
}) => {
  const api = useFileUploadContext();
  const item = useFileUploadItemContext();
  return () => !item.file.type.match(props.type ?? '.*') ? null : createVNode(ark.div, mergeProps(api.value.getItemPreviewProps(item), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'FileUploadItemPreview',
  props: {
    type: {
      type: String,
      default: '.*'
    }
  }
});

export { FileUploadItemPreview };
