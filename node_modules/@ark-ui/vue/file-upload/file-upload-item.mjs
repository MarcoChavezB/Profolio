import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useFileUploadContext } from './file-upload-context.mjs';
import { FileUploadItemProvider } from './file-upload-item-context.mjs';

const FileUploadItem = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useFileUploadContext();
  FileUploadItemProvider(props);
  return () => createVNode(ark.li, mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemProps(props))]
  });
}, {
  name: 'FileUploadItem',
  props: {
    file: {
      type: Object,
      required: true
    }
  }
});

export { FileUploadItem };
