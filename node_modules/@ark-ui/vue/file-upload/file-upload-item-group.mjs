import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useFileUploadContext } from './file-upload-context.mjs';

const FileUploadItemGroup = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useFileUploadContext();
  return () => createVNode(ark.ul, mergeProps(api.value.itemGroupProps, attrs), {
    default: () => [slots.default?.(api.value.files)]
  });
}, {
  name: 'FileUploadItemGroup'
});

export { FileUploadItemGroup };
