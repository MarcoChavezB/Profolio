import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useFileUploadContext } from './file-upload-context.mjs';
import { useFileUploadItemContext } from './file-upload-item-context.mjs';

const FileUploadItemSizeText = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useFileUploadContext();
  const item = useFileUploadItemContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemSizeTextProps(item), attrs), {
    default: () => [slots.default?.() || api.value.getFileSize(item.file)]
  });
}, {
  name: 'FileUploadItemSizeText'
});

export { FileUploadItemSizeText };
