'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const fileUploadContext = require('./file-upload-context.cjs');
const fileUploadItemContext = require('./file-upload-item-context.cjs');

const FileUploadItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = fileUploadContext.useFileUploadContext();
  fileUploadItemContext.FileUploadItemProvider(props);
  return () => vue.createVNode(factory.ark.li, vue.mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemProps(props))]
  });
}, {
  name: 'FileUploadItem',
  props: {
    file: {
      type: Object,
      required: true
    }
  }
});

exports.FileUploadItem = FileUploadItem;
