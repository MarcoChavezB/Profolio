'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const fileUploadContext = require('./file-upload-context.cjs');
const fileUploadItemContext = require('./file-upload-item-context.cjs');

const FileUploadItemPreviewImage = /* @__PURE__ */ vue.defineComponent((_, {
  attrs
}) => {
  const api = fileUploadContext.useFileUploadContext();
  const item = fileUploadItemContext.useFileUploadItemContext();
  const url = vue.ref('');
  api.value.createFileUrl(item.file, src => url.value = src);
  const previewProps = api.value.getItemPreviewImageProps({
    ...item,
    url: url.value
  });
  return () => vue.createVNode(factory.ark.img, vue.mergeProps(previewProps, attrs), null);
}, {
  name: 'FileUploadItemPreviewImage'
});

exports.FileUploadItemPreviewImage = FileUploadItemPreviewImage;
