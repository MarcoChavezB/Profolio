'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const fileUploadContext = require('./file-upload-context.cjs');

const FileUploadItemGroup = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = fileUploadContext.useFileUploadContext();
  return () => vue.createVNode(factory.ark.ul, vue.mergeProps(api.value.itemGroupProps, attrs), {
    default: () => [slots.default?.(api.value.files)]
  });
}, {
  name: 'FileUploadItemGroup'
});

exports.FileUploadItemGroup = FileUploadItemGroup;
