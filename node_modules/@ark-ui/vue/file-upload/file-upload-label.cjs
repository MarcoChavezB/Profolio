'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const fileUploadContext = require('./file-upload-context.cjs');

const FileUploadLabel = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = fileUploadContext.useFileUploadContext();
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'FileUploadLabel'
});

exports.FileUploadLabel = FileUploadLabel;
