'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const fileUploadContext = require('./file-upload-context.cjs');
const fileUpload_props = require('./file-upload.props.cjs');
const useFileUpload = require('./use-file-upload.cjs');

const FileUploadRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useFileUpload.useFileUpload(props, emit);
  fileUploadContext.FileUploadProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'FileUploadRoot',
  props: fileUpload_props.props,
  emits: fileUpload_props.emits
});

exports.FileUploadRoot = FileUploadRoot;
