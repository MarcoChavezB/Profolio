import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useFileUploadContext } from './file-upload-context.mjs';
import { useFileUploadItemContext } from './file-upload-item-context.mjs';

const FileUploadItemName = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useFileUploadContext();
  const item = useFileUploadItemContext();
  return () => createVNode(ark.div, mergeProps(api.value.getItemNameProps(item), attrs), {
    default: () => [slots.default?.() || item.file.name]
  });
}, {
  name: 'FileUploadItemName'
});

export { FileUploadItemName };
