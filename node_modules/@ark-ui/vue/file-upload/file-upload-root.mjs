import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { FileUploadProvider } from './file-upload-context.mjs';
import { props, emits } from './file-upload.props.mjs';
import { useFileUpload } from './use-file-upload.mjs';

const FileUploadRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useFileUpload(props, emit);
  FileUploadProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'FileUploadRoot',
  props,
  emits
});

export { FileUploadRoot };
