import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useFileUploadContext } from './file-upload-context.mjs';

const FileUploadTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useFileUploadContext();
  return () => createVNode(ark.button, mergeProps(api.value.triggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'FileUploadTrigger'
});

export { FileUploadTrigger };
