'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const presenceContext = require('../presence/presence-context.cjs');
const dialogContext = require('./dialog-context.cjs');

const DialogTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = dialogContext.useDialogContext();
  const presenceApi = presenceContext.usePresenceContext();
  const triggerProps = vue.computed(() => ({
    ...api.value.triggerProps,
    'aria-controls': presenceApi.value.isUnmounted ? undefined : api.value.triggerProps['aria-controls']
  }));
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(triggerProps.value, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DialogTrigger'
});

exports.DialogTrigger = DialogTrigger;
