'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const presenceContext = require('../presence/presence-context.cjs');
const presence_props = require('../presence/presence.props.cjs');
const dialogContext = require('./dialog-context.cjs');

const DialogBackdrop = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = dialogContext.useDialogContext();
  const presenceApi = presenceContext.usePresenceContext();
  return () => vue.createVNode(vue.Fragment, null, [presenceApi.value.isUnmounted ? null : vue.createVNode(factory.ark.div, vue.mergeProps(api.value.backdropProps, attrs), {
    default: () => [slots.default?.()]
  })]);
}, {
  name: 'DialogBackdrop',
  props: presence_props.props,
  emits: presence_props.emits
});

exports.DialogBackdrop = DialogBackdrop;
