'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const dialogContext = require('./dialog-context.cjs');

const DialogTitle = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = dialogContext.useDialogContext();
  return () => vue.createVNode(factory.ark.h2, vue.mergeProps(api.value.titleProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DialogTitle'
});

exports.DialogTitle = DialogTitle;
