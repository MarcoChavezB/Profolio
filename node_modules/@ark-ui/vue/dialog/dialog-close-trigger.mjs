import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useDialogContext } from './dialog-context.mjs';

const DialogCloseTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useDialogContext();
  return () => createVNode(ark.button, mergeProps(api.value.closeTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DialogCloseTrigger'
});

export { DialogCloseTrigger };
