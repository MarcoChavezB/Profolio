import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { usePresenceContext } from '../presence/presence-context.mjs';
import { props, emits } from '../presence/presence.props.mjs';
import { useDialogContext } from './dialog-context.mjs';

const DialogBackdrop = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useDialogContext();
  const presenceApi = usePresenceContext();
  return () => createVNode(Fragment, null, [presenceApi.value.isUnmounted ? null : createVNode(ark.div, mergeProps(api.value.backdropProps, attrs), {
    default: () => [slots.default?.()]
  })]);
}, {
  name: 'DialogBackdrop',
  props,
  emits
});

export { DialogBackdrop };
