'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const dialogContext = require('./dialog-context.cjs');

const DialogCloseTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = dialogContext.useDialogContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.closeTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DialogCloseTrigger'
});

exports.DialogCloseTrigger = DialogCloseTrigger;
