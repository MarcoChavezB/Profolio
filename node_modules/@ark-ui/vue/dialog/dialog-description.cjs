'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const dialogContext = require('./dialog-context.cjs');

const DialogDescription = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = dialogContext.useDialogContext();
  return () => vue.createVNode(factory.ark.p, vue.mergeProps(api.value.descriptionProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'DialogDescription'
});

exports.DialogDescription = DialogDescription;
