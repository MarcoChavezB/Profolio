'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const toggleGroupContext = require('./toggle-group-context.cjs');
const toggleGroup_props = require('./toggle-group.props.cjs');
const useToggleGroup = require('./use-toggle-group.cjs');

const ToggleGroupRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useToggleGroup.useToggleGroup(props, emit);
  toggleGroupContext.ToggleGroupProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToggleGroupRoot',
  props: toggleGroup_props.props,
  emits: toggleGroup_props.emits
});

exports.ToggleGroupRoot = ToggleGroupRoot;
