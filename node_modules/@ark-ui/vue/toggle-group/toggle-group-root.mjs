import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { ToggleGroupProvider } from './toggle-group-context.mjs';
import { props, emits } from './toggle-group.props.mjs';
import { useToggleGroup } from './use-toggle-group.mjs';

const ToggleGroupRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useToggleGroup(props, emit);
  ToggleGroupProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToggleGroupRoot',
  props,
  emits
});

export { ToggleGroupRoot };
