'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const toggleGroupContext = require('./toggle-group-context.cjs');

const ToggleGroupItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = toggleGroupContext.useToggleGroupContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToggleGroupItem',
  props: {
    value: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

exports.ToggleGroupItem = ToggleGroupItem;
