import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useToggleGroupContext } from './toggle-group-context.mjs';

const ToggleGroupItem = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useToggleGroupContext();
  return () => createVNode(ark.button, mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToggleGroupItem',
  props: {
    value: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean
    }
  }
});

export { ToggleGroupItem };
