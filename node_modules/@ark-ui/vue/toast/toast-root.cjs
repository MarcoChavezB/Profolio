'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const toastContext = require('./toast-context.cjs');

const ToastRoot = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = toastContext.useToastContext();
  return () => vue.createVNode(factory.ark.li, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToastRoot'
});

exports.ToastRoot = ToastRoot;
