'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const toastContext = require('./toast-context.cjs');

const ToastDescription = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = toastContext.useToastContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.descriptionProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToastDescription'
});

exports.ToastDescription = ToastDescription;
