'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const toastContext = require('./toast-context.cjs');

const ToastTitle = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = toastContext.useToastContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.titleProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToastTitle'
});

exports.ToastTitle = ToastTitle;
