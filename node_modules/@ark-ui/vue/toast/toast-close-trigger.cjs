'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const toastContext = require('./toast-context.cjs');

const ToastCloseTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  attrs,
  slots
}) => {
  const api = toastContext.useToastContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.closeTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToastCloseTrigger'
});

exports.ToastCloseTrigger = ToastCloseTrigger;
