import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useToastContext } from './toast-context.mjs';

const ToastTitle = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  const api = useToastContext();
  return () => createVNode(ark.div, mergeProps(api.value.titleProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToastTitle'
});

export { ToastTitle };
