import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useToastContext } from './toast-context.mjs';

const ToastDescription = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  const api = useToastContext();
  return () => createVNode(ark.div, mergeProps(api.value.descriptionProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToastDescription'
});

export { ToastDescription };
