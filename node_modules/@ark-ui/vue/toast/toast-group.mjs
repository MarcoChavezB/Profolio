import { defineComponent, createVNode } from 'vue';
import { ark } from '../factory.mjs';

const ToastGroup = /* @__PURE__ */ defineComponent((_, {
  attrs,
  slots
}) => {
  return () => createVNode(ark.ol, attrs, {
    default: () => [slots.default?.()]
  });
}, {
  name: 'ToastGroup'
});

export { ToastGroup };
