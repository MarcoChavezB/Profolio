'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const environmentContext = require('./environment-context.cjs');

const Environment = /* @__PURE__ */ vue.defineComponent((props, {
  slots
}) => {
  const elRef = vue.ref();
  const currentEnv = vue.computed(() => () => props.value ?? elRef.value?.ownerDocument ?? document);
  const showSpan = !props.value;
  environmentContext.EnvironmentProvider(currentEnv.value);
  return () => vue.createVNode(vue.Fragment, null, [slots.default?.(), showSpan ? vue.createVNode("span", {
    "hidden": true,
    "ref": elRef
  }, null) : null]);
}, {
  name: 'Environment',
  props: {
    value: Object
  }
});

exports.Environment = Environment;
