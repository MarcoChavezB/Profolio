import { defineComponent, createVNode, mergeProps } from 'vue';
import { segmentGroupAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { useSegmentGroupContext } from './segment-group-context.mjs';

const SegmentGroupLabel = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useSegmentGroupContext();
  return () => createVNode(ark.label, mergeProps(api.value.labelProps, segmentGroupAnatomy.build().label.attrs, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SegmentGroupLabel'
});

export { SegmentGroupLabel };
