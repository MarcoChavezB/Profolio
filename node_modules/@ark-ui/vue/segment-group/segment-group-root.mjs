import { defineComponent, createVNode, mergeProps } from 'vue';
import { segmentGroupAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { SegmentGroupProvider } from './segment-group-context.mjs';
import { props, emits } from './segment-group.props.mjs';
import { useSegmentGroup } from './use-segment-group.mjs';

const SegmentGroupRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useSegmentGroup(props, emit);
  SegmentGroupProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, segmentGroupAnatomy.build().root.attrs, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SegmentGroupRoot',
  props,
  emits
});

export { SegmentGroupRoot };
