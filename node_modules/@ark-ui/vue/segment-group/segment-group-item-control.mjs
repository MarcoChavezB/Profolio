import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { segmentGroupAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { useSegmentGroupContext } from './segment-group-context.mjs';
import { useSegmentGroupItemContext } from './segment-group-item-context.mjs';

const SegmentGroupItemControl = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useSegmentGroupContext();
  const itemProps = useSegmentGroupItemContext();
  return () => createVNode(Fragment, null, [createVNode(ark.div, mergeProps(api.value.getItemControlProps(itemProps.value), segmentGroupAnatomy.build().itemControl.attrs, attrs), {
    default: () => [slots.default?.()]
  }), createVNode("input", api.value.getItemHiddenInputProps(itemProps.value), null)]);
}, {
  name: 'SegmentGroupItemControl'
});

export { SegmentGroupItemControl };
