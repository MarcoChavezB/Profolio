import { defineComponent, createVNode, mergeProps } from 'vue';
import { segmentGroupAnatomy } from '@ark-ui/anatomy';
import { ark } from '../factory.mjs';
import { useSegmentGroupContext } from './segment-group-context.mjs';
import { useSegmentGroupItemContext } from './segment-group-item-context.mjs';

const SegmentGroupItemText = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useSegmentGroupContext();
  const itemProps = useSegmentGroupItemContext();
  return () => createVNode(ark.span, mergeProps(api.value.getItemTextProps(itemProps.value), segmentGroupAnatomy.build().itemText.attrs, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'SegmentGroupItemText'
});

export { SegmentGroupItemText };
