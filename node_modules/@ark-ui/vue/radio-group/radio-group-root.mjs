import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { RadioGroupProvider } from './radio-group-context.mjs';
import { props, emits } from './radio-group.props.mjs';
import { useRadioGroup } from './use-radio-group.mjs';

const RadioGroupRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useRadioGroup(props, emit);
  RadioGroupProvider(api);
  return () => createVNode(ark.div, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'RadioGroupRoot',
  props,
  emits
});

export { RadioGroupRoot };
