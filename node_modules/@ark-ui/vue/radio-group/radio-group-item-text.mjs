import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useRadioGroupContext } from './radio-group-context.mjs';
import { useRadioGroupItemContext } from './radio-group-item-context.mjs';

const RadioGroupItemText = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useRadioGroupContext();
  const itemProps = useRadioGroupItemContext();
  return () => createVNode(ark.span, mergeProps(api.value.getItemTextProps(itemProps.value), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'RadioGroupItemText'
});

export { RadioGroupItemText };
