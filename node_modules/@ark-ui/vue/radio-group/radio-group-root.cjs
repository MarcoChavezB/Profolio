'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const radioGroupContext = require('./radio-group-context.cjs');
const radioGroup_props = require('./radio-group.props.cjs');
const useRadioGroup = require('./use-radio-group.cjs');

const RadioGroupRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = useRadioGroup.useRadioGroup(props, emit);
  radioGroupContext.RadioGroupProvider(api);
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'RadioGroupRoot',
  props: radioGroup_props.props,
  emits: radioGroup_props.emits
});

exports.RadioGroupRoot = RadioGroupRoot;
