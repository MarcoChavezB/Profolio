'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const radioGroupContext = require('./radio-group-context.cjs');
const radioGroupItemContext = require('./radio-group-item-context.cjs');

const RadioGroupItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = radioGroupContext.useRadioGroupContext();
  radioGroupItemContext.RadioGroupItemProvider(vue.computed(() => props));
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemState(props))]
  });
}, {
  name: 'RadioGroupItem',
  props: {
    value: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean,
      default: undefined
    },
    invalid: {
      type: Boolean,
      default: undefined
    }
  }
});

exports.RadioGroupItem = RadioGroupItem;
