'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const radioGroupContext = require('./radio-group-context.cjs');

const RadioGroupLabel = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = radioGroupContext.useRadioGroupContext();
  return () => vue.createVNode(factory.ark.label, vue.mergeProps(api.value.labelProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'RadioGroupLabel'
});

exports.RadioGroupLabel = RadioGroupLabel;
