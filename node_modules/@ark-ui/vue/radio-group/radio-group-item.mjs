import { defineComponent, computed, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useRadioGroupContext } from './radio-group-context.mjs';
import { RadioGroupItemProvider } from './radio-group-item-context.mjs';

const RadioGroupItem = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = useRadioGroupContext();
  RadioGroupItemProvider(computed(() => props));
  return () => createVNode(ark.label, mergeProps(api.value.getItemProps(props), attrs), {
    default: () => [slots.default?.(api.value.getItemState(props))]
  });
}, {
  name: 'RadioGroupItem',
  props: {
    value: {
      type: String,
      required: true
    },
    disabled: {
      type: Boolean,
      default: undefined
    },
    invalid: {
      type: Boolean,
      default: undefined
    }
  }
});

export { RadioGroupItem };
