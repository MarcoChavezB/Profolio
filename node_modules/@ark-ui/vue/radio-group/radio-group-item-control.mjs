import { defineComponent, createVNode, Fragment, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { useRadioGroupContext } from './radio-group-context.mjs';
import { useRadioGroupItemContext } from './radio-group-item-context.mjs';

const RadioGroupItemControl = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = useRadioGroupContext();
  const itemProps = useRadioGroupItemContext();
  return () => createVNode(Fragment, null, [createVNode(ark.div, mergeProps(api.value.getItemControlProps(itemProps.value), attrs), {
    default: () => [slots.default?.()]
  }), createVNode("input", api.value.getItemHiddenInputProps(itemProps.value), null)]);
}, {
  name: 'RadioGroupItemControl'
});

export { RadioGroupItemControl };
