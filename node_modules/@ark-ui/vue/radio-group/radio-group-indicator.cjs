'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const radioGroupContext = require('./radio-group-context.cjs');

const RadioGroupIndicator = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = radioGroupContext.useRadioGroupContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.indicatorProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'RadioGroupIndicator'
});

exports.RadioGroupIndicator = RadioGroupIndicator;
