'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const paginationContext = require('./pagination-context.cjs');

const PaginationNextTrigger = /* @__PURE__ */ vue.defineComponent((_, {
  slots,
  attrs
}) => {
  const api = paginationContext.usePaginationContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.nextTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PaginationNextTrigger'
});

exports.PaginationNextTrigger = PaginationNextTrigger;
