'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const paginationContext = require('./pagination-context.cjs');
const pagination_props = require('./pagination.props.cjs');
const usePagination = require('./use-pagination.cjs');

const PaginationRoot = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = usePagination.usePagination(props, emit);
  paginationContext.PaginationProvider(api);
  return () => vue.createVNode(factory.ark.nav, vue.mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'PaginationRoot',
  props: pagination_props.props,
  emits: pagination_props.emits
});

exports.PaginationRoot = PaginationRoot;
