import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { PaginationProvider } from './pagination-context.mjs';
import { props, emits } from './pagination.props.mjs';
import { usePagination } from './use-pagination.mjs';

const PaginationRoot = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs,
  emit
}) => {
  const api = usePagination(props, emit);
  PaginationProvider(api);
  return () => createVNode(ark.nav, mergeProps(api.value.rootProps, attrs), {
    default: () => [slots.default?.(api.value)]
  });
}, {
  name: 'PaginationRoot',
  props,
  emits
});

export { PaginationRoot };
