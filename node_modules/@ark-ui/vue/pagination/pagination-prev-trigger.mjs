import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { usePaginationContext } from './pagination-context.mjs';

const PaginationPrevTrigger = /* @__PURE__ */ defineComponent((_, {
  slots,
  attrs
}) => {
  const api = usePaginationContext();
  return () => createVNode(ark.button, mergeProps(api.value.prevTriggerProps, attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PaginationPrevTrigger'
});

export { PaginationPrevTrigger };
