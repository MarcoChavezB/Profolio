'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const paginationContext = require('./pagination-context.cjs');

const PaginationEllipsis = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = paginationContext.usePaginationContext();
  return () => vue.createVNode(factory.ark.div, vue.mergeProps(api.value.getEllipsisProps({
    index: props.index
  }), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PaginationEllipsis',
  props: {
    index: {
      type: Number,
      required: true
    }
  }
});

exports.PaginationEllipsis = PaginationEllipsis;
