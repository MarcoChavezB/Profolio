'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const vue = require('vue');
const factory = require('../factory.cjs');
const paginationContext = require('./pagination-context.cjs');

const PaginationItem = /* @__PURE__ */ vue.defineComponent((props, {
  slots,
  attrs
}) => {
  const api = paginationContext.usePaginationContext();
  return () => vue.createVNode(factory.ark.button, vue.mergeProps(api.value.getItemProps({
    type: 'page',
    value: props.value
  }), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PaginationItem',
  props: {
    type: {
      type: String,
      default: 'page'
    },
    value: {
      type: Number,
      required: true
    }
  }
});

exports.PaginationItem = PaginationItem;
