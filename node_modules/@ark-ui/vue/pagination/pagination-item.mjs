import { defineComponent, createVNode, mergeProps } from 'vue';
import { ark } from '../factory.mjs';
import { usePaginationContext } from './pagination-context.mjs';

const PaginationItem = /* @__PURE__ */ defineComponent((props, {
  slots,
  attrs
}) => {
  const api = usePaginationContext();
  return () => createVNode(ark.button, mergeProps(api.value.getItemProps({
    type: 'page',
    value: props.value
  }), attrs), {
    default: () => [slots.default?.()]
  });
}, {
  name: 'PaginationItem',
  props: {
    type: {
      type: String,
      default: 'page'
    },
    value: {
      type: Number,
      required: true
    }
  }
});

export { PaginationItem };
